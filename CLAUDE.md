# sts2core — 《杀戮尖塔 2》模拟内核（L1 规则 + L2 求解器）

零依赖的 Rust 内核。它**已经替换掉**旧的 Python 模拟器（`sts2sim/`，2026-09-12 删除）。
实战驱动流程在父目录 [`../CLAUDE.md`](../CLAUDE.md)，**先读那份**。

## 六份文档，各管一件事

| 文档 | 管什么 | 什么时候看 |
|---|---|---|
| **本文件** | 设计、不变量、接口、验收怎么跑 | 动代码之前，**从头读到尾** |
| [`docs/verified-rules.md`](docs/verified-rules.md) | **现在认定为真的规则**：伤害管线、玩家给的判定、验证器抓到过的真错误 | 要改一个数字、或要加一条规则之前，**先读这份** |
| [`docs/verification-log.md`](docs/verification-log.md) | **证据**：哪一天做了什么、当时读数多少、先做错了什么。**只有带日期的观测，没有规则** | 上面那条规则是被哪一帧钉死的 —— 拿日期回来搜 |
| [`docs/roadmap.md`](docs/roadmap.md) | 做完了什么 / 还没做 / **故意**没做 | 决定下一步做什么的时候 |
| [`docs/trace-format.md`](docs/trace-format.md) | trace 格式 + 四条从 mod 源码读出来的硬约束 | 动录制器或验证器之前 |
| [`docs/relic-hook-taxonomy.md`](docs/relic-hook-taxonomy.md) | 遗物**按钩子面分类**：每件卡在哪个机制上。**不报进度，只报分类** | 要建模遗物之前 |

**本文件不记流水账。** 「哪天做了什么、当时读数多少、我先做错了什么」一律在
verification-log 里，这里只写**现在的样子和为什么是这个样子**。

**verification-log 也不许写规则。** 2026-09-05 立的追加纪律：那边只许写带日期的
观测，任何「X 是 Y」的陈述句都属于 verified-rules。理由是当时量出来的 ——
规则摘要和证据混在一个文件里存了半个月，摘要里的「缩小 ×2/3」早在 08-21 就被
同一个文件里的另一条推翻了，而没有任何东西会因此变红。

---

## 为什么重写（这是设计的全部动机）

旧的 `sts2sim/`（约 3000 行 Python，**2026-09-12 删除**）有三个硬伤，根子都在"状态是对象图、
效果是硬编码"：

1. **只认 35 张牌**，条件牌（`每当`/`如果`/X 费/手牌操作）**拒绝评分**而不是打分
2. **`approx=yes` 全表** —— 节点预算一到就退化成贪心，20+ 张牌时每行都是启发式
3. **敌人模型只有 HP / 每回合伤害 / 4 个 trait**，复活、召唤、无实体、激怒、
   伤害上限全表达不了

2026-08-14 那局死在第 3 幕 Boss 实验体，就是第 3 条的直接后果：`inspect_fight`
报 `traits=[none]`，而它实际有三阶段、隔回合无实体、激怒。搜索再快也没用 ——
搜的是一个不存在的游戏。

> **教训：一个自信地算错的模拟器，比没有模拟器更危险。**

---

## 分层

```
L1  step(state, action) -> state     ← 本目录。纯函数内核，唯一持有游戏规则
L2  局内求解器                        调用 L1，搜"这回合/这场仗怎么打"
L3  构筑顾问                          调用 L2，搜"拿哪张牌/移除/升级/路线"
```

**L2 和 L3 不许自己实现任何游戏规则**，只能经由 L1 的**五个**入口：

```rust
pub fn step(s: State, a: Action) -> State;
pub fn legal_actions(s: &State) -> ([Action; MAX_ACTIONS /* 128 */], usize);
pub fn begin_combat(s: State) -> State;
pub fn end_turn_with_incoming(s: State, inc: &Incoming) -> State;
pub fn grant_relic(s: &mut State, def: &RelicDef, counter: Option<i32>);  // L3 开仗用
```

第四个是给 L2 的：结束回合，但**敌人这一手打多少由调用方给定**。
没有它，L2 就得自己写"格挡怎么吃这几下伤害"—— 那就是在 L2 里复制规则。
它和 `enemy_turn` 共用 `take_attack_hit`，所以格挡逐次吸收、孤注一掷、
火焰屏障、`hp_lost_this_turn` 记账全是同一条路，**不可能长歪**。

第五个是给 L3 的（2026-09-09 加）：把一件遗物的战斗层状态交给玩家实体。
**「哪件遗物给哪几个 status」是游戏规则，所以它在 L1** —— 让 L3 自己照
`RelicDef` 抄一遍就是把规则复制到上层。它必须在 `begin_combat` **之前**调用：
好几件遗物的效果挂在第 1 回合的 `TurnStart` 上（赤牛的活力、锚的格挡）。

**L3 四块全落地了**：合成战斗构造器 `src/synth.rs`
（`(牌组, 遗物, 血量, 遭遇, 种子) -> State`，2026-09-09）· 单场评估
`src/synth/eval.rs`（`(FightSpec, N) -> 死亡率 / 血量分布 / 截断率`，2026-09-10）·
**整幕链式评估** `src/synth/act.rs`（`(牌组, 一条路线) -> 走完这一幕的死亡率`，
2026-09-12）· **MCP 接线** `src/bin/advise.rs` + `tools/advise_core.py`
（2026-09-12），见下面那四节。

**`sts2sim/` 2026-09-12 整个删掉了**，两样东西搬进了本仓库：

* **MCP 外壳** -> `advisor/server.py`（八个工具名和签名一个没动，内部全调这边）。
  它是 L3 接口的**消费者**，该和接口住在一起 —— 见「L3：MCP 接线」那一节的
  「为什么它住在这里」。
* **实测血量** -> `data/enemies_observed_legacy.json`（2026-08-14 那一局记的，
  35 只里 22 只对拍语料里没有 —— 语料 08-16 才开始。**实测不可再生**）。

那个 Python 模拟器本身连同它的两个记录器一起删了：留着一个没人调、结论又和内核
不一致的第二套估值器，比没有更危险。
**解释器也跟着搬了**：`.venv` 现在在本仓库根下（`uv` 按 `pyproject.toml` 建的），
所有 Python 工具走 `D:\game mod\sts2core\.venv\Scripts\python.exe`。

---

## 不变量

改代码前先确认没有破坏这些。它们是性能和正确性的地基：

1. **`State` 保持 POD + `Copy`。** 不许出现 `Vec` / `HashMap` / `Box` / 引用，
   clone 必须是一条 memcpy。**当前 3488 字节**（`N_STATUS` 128 -> 144 之后从 3296 涨上来的
   —— 2026-09-09 一天加了 10 个 status，119 顶到 129），
   `state_is_a_value` 守着 4 KB 上限。
   **`CardInst` 是 8 字节 × 128 张 = 1024 字节，占三分之一** ——
   2026-09-06 给附魔加两个字段时先直接加，`State` 当场 3552、单线程掉约 7%；
   改成把 `flags` 从 `u16` 窄到 `u8`（只有 5 个位）就一个字节都没涨。
   `solver.rs` 里一条 `size_of::<CardInst>() == 8` 的编译期断言守着这件事。
2. **所有伤害走 `damage.rs`。** 不许在别处零散写 `hp -= x`。乘区顺序只在那一处定义。
3. **卡牌/敌人/能力是数据不是代码**（`content.rs` 的 `CardDef`/`EnemyDef`/`PowerDef`）。
   加一张牌应该是加一行表，不是加一个 `if`。加不进去就说明 `Op` 枚举缺原语 ——
   **扩 `Op`，不要在 `step.rs` 里特判卡名。**
4. **RNG 在状态里**（`state.rs` 的 `Rng`，splitmix64，**shuffle / enemy / gen 三条分流**）。
   分流不是洁癖：合流的话「多打一张添柴」会改掉后面所有抽牌。
   **内核的随机序列故意不和游戏一致，也做不到** —— 理由和它为什么不影响任何结论，
   见 verification-log 的「随机数」一节。
   同种子 + 同动作序列 ⇒ 状态逐字节相同（`deterministic_given_seed` 守着）。
   这是配对随机数比较（CRN）和回放验证的前提。
5. **`step` 是全函数**：非法动作返回原状态，不 panic、不报错。
6. **子选择用 `Pending` 建模**，不要让 `step` 变成可重入的。
   `Pending != None` 时 `legal_actions` 只返回 `Action::Choose`。

---

## L1：伤害管线（`damage.rs`）

全内核最高杠杆的一处。乘区顺序是从实战日志反推、再被对拍钉死的。

> **先看这一条**：下面 2–6 那五个乘区（腐化 + 缩小/虚弱/易伤/缓慢）**只对"攻击"
> 生效**。[源码] 它们各自第一句都是 `if (!props.IsPoweredAttack()) return 1m;`，
> 而 `IsPoweredAttack = Move && !Unpowered`。所以**药水 / 遗物 / 能力牌打出的伤害
> 一概不过它们**，走 `damage::apply_modifiers_unpowered`。
> **7 和 8（难以杀灭 / 无实体）没有这条 gate，两条路都要过。**

```
1. 卡面基础值（含升级、含附魔加值）
2. 腐化 ×1.5        ← 作用在基础值上，在力量【之前】
3. + 锋利加值 + 力量 + 活力
   ---- 以下乘区【累乘，最后只取整一次】 ----
3.5 攻击方缩小 ×0.7
4.  攻击方虚弱 ×0.75
5.  防御方易伤 ×1.5
6.  防御方缓慢 ×(1 + 0.1 × 本回合此牌之前打出的牌数)
   ---- 取整 ----
7. 难以杀灭  min(d, cap)
8. 无实体    d = 1
9. 扣格挡，余下进 HP
```

> **不要凭直觉改这张表里的数字。** 「每一步都向下取整」这条曾经写在这里，是错的，
> 被第 1 幕 Boss 的一帧推翻。全部证据在
> [verified-rules 的 1.1](docs/verified-rules.md#11-步骤-356-累乘只在最后取整一次)。

**活力有一条会改变出牌顺序的规矩：多段牌的每一段都吃满加值。**
[源码] `AttackCommand.cs` 的 `Hook.BeforeAttack`/`AfterAttack` 都在多段 `for`
循环**外面**，所以活力挂在整条 `AttackCommand` 上：8 活力配双重打击（5x2）是
**(5+8)x2 = 26**，不是 5x2+8。单段牌上两种读法给出相同的数，只有多段牌分得开。
另一半：活力是 `Counter` 型、**没有回合末衰减**（没花掉的跨回合留着），
但**一张攻击牌打完整个清零**。

### 段数也可以是**算出来的**

伤害管线管的是「每一段打多少」，**段数是另一件事**。今天只有一张牌的段数不是常数：

扯碎（[源码] `TearAsunder`）`WithHitCount(Calculate(target))`，
`Calculate = CalculationBase(0) + CalculationExtra(1) × (1 + M)` = **1 + M**，
`M = State::hp_loss_hits`（本场挨穿过几次）。所以它走 `Op::DamagePerHpLossHit`
而不是 `Scale` —— `Scale` 那一族改的是**基础伤害**，改不了段数。

三条容易写错的：

* **段数在打出的那一刻就定死**（源码里是构造 `AttackCommand` 时求值）。
  写在循环里重读的话，敌人带荆棘时每一段反弹都会把计数器顶上去，
  这张牌会自己越滚越长。
* **`hp_loss_hits` 数的是次数不是血量**，判据是 [源码] 的
  `DamageReceivedEntry` 且 `UnblockedDamage > 0` —— 被完全格挡的不算，
  而来源一概不问（敌人打的、荆棘反弹的、放血那种自伤全算）。
  收口在 `step::after_player_hp_lost`。
* **卡面上那个「（命中3次）」是快照，不是定义。** 游戏把算好的段数渲染进了
  描述文本，照它写死 `hits: 3` 是这张牌第一版的错法。反过来这也是内核唯一的
  观测来源：`sync` 从那句话反推 `hp_loss_hits`（`replay::observed_tear_hits`），
  牌不在手上时那一栏是 0，**方向是低估**。

---

### 附魔

一张牌可以带一个附魔（[源码] `CardModel.Enchantment`，至多一个），
观测里从 2026-09-01 起有这个字段（父目录的第二个补丁），
**牌堆里的牌从 2026-09-03 起也有**（第三个补丁）。

那半年多的空档是有代价的：**同一张牌在手里给 10、在抽牌堆里给 8**，
段内被抽出来的那一张就按 8 算 —— `act1_f14_phantasmal_gardeners` 的整回合
对拍就红在这里。老 trace 拿不到这个字段时**一律按没附魔算**（方向是低估），
和 `draw_order` 缺失时的回退是同一个处置。

内核侧是 `CardInst.ench`（`content::ENCHANTS` 的下标+1）+ `ench_amt`（`Amount`），
**`State` 一个字节没涨** —— `flags` 从 `u16` 窄到 `u8`（只有 5 个位）腾出来的地方
正好装下这两个字节，`CardInst` 仍然是 8 字节。`solver::card_ident` 里有一条
`size_of::<CardInst>() == 8` 的编译期断言守着这件事（它的位宽是精确的，不是哈希）。

2026-09-06 之前这里是一个 `block_bonus: i8` —— **装得下的只有五个钩子里的一个**，
其余（伤害加/乘、关键字）一律当成"没有效果"，而那些的**方向并不统一**：
锋利是低估、腐化是低估、**王室认证是把一张该保留的牌弃掉**。

### 这张表的形状 = `EnchantmentModel` 的虚方法表

[源码] 附魔只有六个口子能改游戏：`EnchantBlockAdditive` / `EnchantBlockMultiplicative` /
`EnchantDamageAdditive` / `EnchantDamageMultiplicative` / `EnchantPlayCount`，
外加 `OnEnchant`（改关键字和费用）和 `OnPlay`（打出时多做一件事）。

`content::ENCHANTS` **只装内核今天真的消费的那四个**：格挡加 · 伤害加 · 伤害乘 ·
关键字。**其余的口子没有字段** —— 加一个没人读的字段和加一个空钩子是同一种错，
它看起来像建好了。三个消费点各自收口：`Op::Block`（`ench_block_add`）·
`resolve_ops` 的 `bonus` 和 `base_mul`（`ench_damage_add` / `ench_damage_mul`，
后者和老的 `F_CORRUPT` 是同一个口子）· `replay::push` 把关键字落进 `CardInst::flags`。

**22 种全部进表**，`modelled` 那一列才是进度（现数见「内容清单」）。
没建全的会被**两处印出来**：`verify` 的「没建全的附魔」和 `solve --live` 的
「附魔」那一行（连同它缺哪一个机制）。
**这两处 2026-09-06 才补** —— 在那之前 `Report::unknown_enchantments`
只往结构体里写、没有任何地方印，和「没映射的 status」是同一类静默洞。
建全的：灵巧（格挡+`Amount`）· 锋利（伤害+`Amount`）· 直觉（伤害×2）·
**王室认证（固有+保留）** · 沉稳（保留）。
没建全的每一条都写着**卡在哪一个还没有的机制**，
`replay` 把它们点名进 `Report::unknown_enchantments`（表里没有、和有但没建全，
对内核是同一件事）。

**id 是从类名推的**（`RoyallyApproved` -> `ROYALLY_APPROVED`），
观测里见过的两个都对上了，其余 20 个对不上会被点名，不会静默算错。

### 保留和固有：附魔的第四个口子有真消费点了

`F_RETAIN` / `F_INNATE` 两个 flag 位一直存在、**一直没人读**（路线图里挂着
"flag 位有了，`step` 还没用"）。王室认证给的正是这两个，于是它们各自接上了：

* **保留** —— `step::end_turn_impl` 弃手牌那个循环跳过带 `F_RETAIN` 的牌。
  [实测] 2026-09-06 `act3_f46_elite_soul_nexus` 两个回合边界：带王室认证的均衡+
  留在手上，同一手的添柴+ 被弃掉。**和均衡的 `St::Entrench` 不是一回事**：
  那个保整手牌、只保一个回合。
* **固有** —— `step::begin_combat` 洗完之后把它们挪到牌堆顶（内核的顶在**末尾**）。
  多张固有牌之间的相对顺序**没有依据**，保持原有先后，不另外洗一次。

---

## L1：触发器（`Hook` / `PowerDef`）

**能力牌就是带层数的 status**，层数 = 每次触发的效果值（激怒 2 = 每次 +2 力量）。
规则写在 `content::POWERS`，`step::fire(s, hook)` 在固定几个点上把它们跑一遍 ——
`fire()` **不认识任何具体 status**。

`every_power_card_has_a_rule_and_no_rule_is_claimed_twice` 守着这条：挂了 status
却没人认领的能力牌是张哑弹，不加这个测试没人会发现。姊妹测试
`no_status_handed_out_by_a_relic_or_an_enemy_is_a_dud` 把口径推广到遗物和敌人 ——
它已经真的抓到过哑弹（荆棘、敌人持有的覆甲）。

### 21 个钩子，每个都有真牌在用

**没有为将来预留的空钩子**，这是一条规矩：加钩子必须连着它的消费者一起加。

| Hook | 触发点 | 消费者（`content::POWERS` 里的 `st`，共 73 条） |
|---|---|---|
| `TurnStart` | **我的**回合开始，能量回满后、抽牌前 | 28 条。恶魔形态 绯红披风 滚石 盾墙 覆甲(玩家掉层，**第 2 回合起**) 巨像 好勇斗狠 抱抱先生 水银沙漏 灯笼 锚 弹珠袋 红面具 准备背包 烛台 开心小花 花粉核心 佩尔之肉 光耀 赤牛 摆动球 舵盘 **号角靴钉 宝石面具 碎石者 小血瓶 缩放仪 古茶具** |
| `HandDrawn` | **我的**回合开始、**手牌已经发下来之后**（`open_hand` 末尾） | **风箱 / 骨茶**（开局升级手牌）。和 `TurnStart` 差的就是抽牌那一步 —— 挂在那个钩子上的话它会去升级一手还没发下来的空牌。[源码] 那两件是 `AfterPlayerTurnStart`，而宝石面具是 `BeforeHandDraw`，**这一步分得开这两批** |
| `TurnEnd` | **我的**回合结束，弃手牌前、敌人行动前 | 招架盾 **尖叫酒壶** **斗篷扣**(每张手牌 1 格挡) 覆甲(玩家给格挡) 奥利哈钢 再生 惊逃 缠绕 娇弱 历石 轰鸣 |
| `TurnEndVeryEarly` | 我的回合结束，**在 `TurnEnd` 之前** | 奥利哈钢(快照) |
| `EnemyTurnStart` | **敌人**整边开始，清完格挡、第一只出手之前 | 覆甲(敌人掉层) 适生力 **幻象(回满血)** **沙坑(即死倒计时)** |
| `EnemyTurnEnd` | **敌人**整边行动完之后 | 高压 仪式 覆甲(敌人给格挡) 领地意识 逃跑大师 天敌 |
| `PlayerSkill` | 打出技能牌，**牌结算之前** | 激怒（敌人持有）开信刀 活力火花 |
| `PlayerAttack` | 打出攻击牌，**牌结算之后** | 狂怒 精致折扇 苦无 杂耍 |
| `CardPlayed` | 打出任意牌 | 凋萎存在 娇弱 |
| `CardExhausted` | 所有进消耗堆的路径 | 无惧疼痛 黑暗之拥 |
| `GainBlock` | **获得格挡且数值 > 0**，四条加格挡的路径全收口在 `step::gain_block`。**`ctx` = 谁获得了格挡**（玩家是 `usize::MAX`）| 势不可当（[源码] 只认自己那一份）|
| `ApplyVuln` | 我给敌人上易伤，**每个吃到的敌人各一次** | 凶恶 |
| `PlayerLoseHp` | 我方回合内因卡牌失去生命 | 撕裂 |
| `PlayerDamaged` | 我挨伤害 | 百年积木 |
| `Attacked` | 挨敌人一次攻击，**多段各一次**（`ctx` = 攻击者） | 火焰屏障 荆棘 |
| `EnemyAttacked` | 敌人发起攻击 | 荆棘 |
| `EnemyDamaged` | 敌人挨我一下，**伤害落地之后**（`ctx` = 挨打的那只，**且只有它触发**） | 蜷身 扑翼 尖叫 耕地 |
| `EnemyDied` | 一只敌人死 | 寄生物 适生力 **幻象** 库存 地精之角 |
| `AllyDied` | 同伴死 | 蟹之怒 **贪食**(噬尸蛞蝓：+力量 且被自己的进食击晕一回合) |
| `CombatVictoryEarly` | 敌人清空、玩家活着，**第一段** | 带骨肉 |
| `CombatVictory` | 同上，**第二段** | 燃烧之血 |

> **薪火之源不在这张表里。** `St::Pyre` 只是显示印记（`MARKER_STATUSES`），
> 真实效果是 `Op::GainMaxEnergy`（`s.base_energy += n`，**永久**，不衰减不带条件）。
> 能力的价值因此有两种结构：**永久资源**和**每回合触发** —— 叶评估的公式得分开写。

### 三条容易写错的

* **`Attacked` 和 `EnemyDamaged` 的 `ctx` 不是一回事。** 前者持有者是玩家、ctx
  是攻击者（火焰屏障要反伤谁）；后者持有者就是 ctx 那只敌人本身。所以 `fire_ctx`
  里对 `EnemyDamaged` 有一条窄判断：**只对挨打的那只触发**。写错了会让打一个敌人
  导致全场一起加格挡，而且**只在多怪场才看得出来** ——
  `curl_up_fires_only_on_the_enemy_that_was_hit` 守着。
* **`PlayerSkill` 在结算之前、`PlayerAttack` 在结算之后**，这个不对称是刻意的：
  前者保住激怒已经验证过的行为，后者是保守选择（全身撞击吃不到自己那一下狂怒
  给的格挡）。
* **钩子归属（给玩家发还是给敌人发）一刀切不了。** `fire` 对绝大多数钩子两边都发，
  而真实规则里两种组合都存在：**盾墙是敌人持有、挂在玩家回合开始**；
  **覆甲同一个 status 两边都能持有、时序却完全不同**（玩家在我的回合末给格挡，
  敌人在它自己的回合末给）。所以门开在**规则自己**身上 ——
  `TCond::OwnerIsPlayer` / `OwnerIsEnemy`，**别去改 `fire_ctx` 的分发**。

**钩子可以套钩子**，由 `MAX_HOOK_DEPTH = 4` 兜底。绯红披风回合开始掉 1 点血会唤醒
撕裂 —— 这是放血流的核心组合，不接上等于把一整套构筑算废。
（最初为了躲死循环写成"钩子不套钩子"，是错的，玩家指出来的。）

> **那个兜底要真的盖到伤害路径上。** `hit_enemy_with` 原来把
> `EnemyAttacked` / `EnemyDamaged` / `EnemyDied` / `AllyDied` 一律按 `depth = 0`
> 点火，于是「触发器打人 -> 挨打的那只再触发 -> 再打人」的环**每绕一圈层数就被
> 重置**，`MAX_HOOK_DEPTH` 形同虚设 —— 表现是**爆栈**，不是一条红。
> 2026-09-12 修成：打牌那条路传 0（逐字同旧），`run_ops` 那条传 `depth + 1`。
> 同一天的另一半是势不可当的归属（见 `GainBlock` 那一行）。
> **两条都是 `bin/act_eval` 照出来的，前八条验收全绿。**

代价：`fire()` 吃掉约 12% 的单线程吞吐。**要优化的话第一步是按 hook 给 `POWERS`
分组**（现在是整表线性扫），别先动 `State` 的布局。

---

## L1：三个类别，别混淆

"卡牌带来的行为"分三类，各有各的表。加牌前先想清楚属于哪一类：

| 类别 | 表 | 能表达什么 |
|---|---|---|
| 触发器 | `POWERS` | 「在某个时点**多做**一件事」 |
| 规则修饰 | `RULE_MODIFIERS` | 「**不要做**某件事」/ 直接改判定 |
| 手牌发作 | `HAND_END` | 「回合结束时这张牌**还在手上**才发生」 |
| 消耗堆发作 | `EXHAUST_END_AUTOPLAY` | 「回合结束时这张牌**在消耗堆里**就自己打出来」 |
| 附魔 | `ENCHANTS` | 「这**一张实例**的格挡/伤害/关键字被改过」 |

`EXHAUST_END_AUTOPLAY` 是 `HAND_END` 的姊妹表，今天各只有一张牌在用
（彼岸咆哮 / 凋萎）。时点由 [源码] `CombatManager.EndPlayerTurnPhaseOneInternal`
定死：`AutoPostPlay` 阶段跑在 `Hook.BeforeTurnEnd` 和弃手牌**之前**。

`RULE_MODIFIERS` 这张表**不带行为**，消费点是 `step.rs` / `damage.rs` 里几个写死的
窄 `if`（壁垒不清格挡、均衡不弃手牌、孤注一掷立刻死、臂甲和坚定不移的充能、失衡）。
它存在的意义是让这个类别**可枚举**，好让完整性测试区分"故意写死的规则修饰"和
"忘了写规则的哑弹"。**这类牌要是多起来，该给它们一张真正带行为的表，
而不是继续往 `step.rs` 塞 `if`。**

`TURN_SCOPED` 里的 status 在**我的下一个回合开始时**清零，不是回合结束时 ——
火焰屏障要挡的正是敌人回合那几下。

---

## L1：敌人侧

* **召唤** `EOp::Summon`：新敌人用它自己 `EnemyDef` 的 `start_status`
  （爪牙标记就在那儿），且**当回合不行动**。
* **爪牙随主死** `step::no_master_left`：**非爪牙全死光，战斗就结束**，场上剩的
  爪牙跟着消失。两次独立实测（雾菇→利齿之眼、同族神官→两个信徒）。不实现这条，
  L2 会把第 1 幕 Boss 估成 307 血而不是实际要打的 190。
  兜底：场上从没出现过非爪牙时退回普通规则，免得一场纯爪牙的仗开局就判结束。
* **敌人塞状态牌** `EOp::AddCardToDiscard`：史莱姆的 `StatusCard:N` 往我的
  **弃牌堆**塞 N 张黏液（不是手牌）。张数取自意图标签，**只有树枝史莱姆（中）
  那一手实测过**，其余是推的。
* **同名的多个 `EnemyDef` 会互相盖住** —— 名字到 def 是 `position(|d| d.name == n)`，
  第一个匹配就返回。巨斧机器人的「三具身体共用一个 def」因此只能由**层数驱动**
  差异（`TOp::SummonCarryingSelfMinusOne` + `EOp::SelfStatusPerStack`）。

`CardExhausted` 靠 `step::exhaust_card` 收口 —— 内核里有三条进消耗堆的路径
（卡面自带消耗、恶魔之焰清手牌、烙印选牌），**任何新路径都必须走它**。

### 「还在场上」不等于「活着」，一共三处口径

带适生力的实验体被砍死之后血是 0、打不到、**观测里也看不见它**，
但战斗不结束（[源码] `AdaptablePower.ShouldStopCombatFromEnding() => true`）。
三处必须一起读，缺一处就是静默的错：

| 处 | 干什么 | 漏了会怎样 |
|---|---|---|
| `State::any_enemy_present` / `step::no_master_left` | 判胜利用 `alive() \|\| Adaptable > 0` | 砍掉第一条命当场判赢 |
| `Replayer::revive_owed` | 记住"消失前它带着适生力" | `sync` 每帧从观测重建，适生力一丢就退化成上一行 |
| `step` 的 `PlayCard`：**没有活敌人时无目标牌照打** | 和 `legal_actions` 保持一致 | 那个窗口里**每一张牌**都被拒，而"拒绝"的表现是状态没变，不报错 |

最后那条是 2026-09-03 才发现的真 bug，被前两条挡在后面很久 ——
详见 verification-log 的「复活窗口那 4 帧」。

### 出招表和 AI 预测

`tools/dump_bestiary.py` 从 sts2.wiki 扒了 115 个敌人的出招表到
`traces/enemies_wiki.json`。**wiki 不是权威，游戏才是** —— 脚本强制自检，把 wiki 的
HP 和实录里量到的逐个对，这才是它可以被当作*假设来源*的理由。

出招表是**状态机**（`EnemyDef::machine`）：反编译出的 121 只里 82 只本来就是纯
确定性的，那些继续用循环；结构性需要机器的逐条照抄了 [源码]。

判决机制是 `verify --predict-enemy`，判的是**「观测到的这一手在不在允许集合里」**，
不是逐字相等 —— 内核的随机流故意和游戏不一致（不变量 4），"下一手是哪一个"本来
就预测不了。它验的是**「下一手是什么」，不是「这一手打多少」**；后者由默认对拍
模式检验。两者要分开看，修的地方完全不同。

---

## L1：进阶（`src/asc.rs`，**生成产物**）

**十档各自改什么、以及为什么只有两档进 L1，是规则，写在
[verified-rules 的 2.7](docs/verified-rules.md)。** 这里只写内核长什么样。

`State::ascension`（0–10）。战斗层只有两档读得到它，**其余八档一条都不该进 L1**：

| 档 | 改什么 | 谁读 |
|---|---|---|
| **A8 `ToughEnemies`** | 敌人**耐久**：血量（含多形态/孵化）· 格挡 · 覆甲 · 几个 status 量 | `asc::hp_range` / `asc::adjust` |
| **A9 `DeadlyEnemies`** | 敌人**输出**：伤害 · **段数** · 力量成长 · 部分格挡 · debuff 量 | `asc::adjust` |

其余的全在局外：A1 精英数 ×1.6 · A2 **远古事件**回血 ×0.8（不是篝火）· A3 金币 ×0.75 ·
A4 药水槽 −1（内核照抄观测的 `max_potion_slots`，不从进阶推）· A5 开局一张升华诅咒 ·
A6 商店价 · A7 卡牌概率 · **A10 第二个 Boss 只给最后一幕**。
逐条出处在 `data/ascension.json` 的 `outside_combat`。

### 一个收口点，六处过它、三处有意不过

```rust
asc::adjust(def, mv, op_ix, asc, op) -> EOp   // asc < 8 时逐字返回原 op，一次表都不查
asc::hp_range(def, asc) -> Option<(i32, i32)> // 血量区间两档
```

读 `EnemyMove::ops` 的地方一共九处。**六处必须过 `adjust`**：`step::enemy_turn`（执行）·
`rollout::fast_play_turn` 和 `rollout::predicted_threat`（两个威胁预测）·
`plan` 的最坏来袭 · `replay::move_signature`（意图标签 —— 游戏在 A8/A9 显示的
标签本来就是缩放过的）· `bin/solve::upgrade_threat_live`。
**三处有意不过**：`bin/plan_audit::move_has_non_attack_ops` 和 `lib.rs` 的两条
完整性测试 —— 它们只看 op 的**种类**，而进阶从不换 `EOp` 的变体。
**漏一处的表现是那条路径静默地按低进阶算。**

`ascension` 从 trace 的 `run.ascension` 进来（`Trace::ascension` 是数、`run` 那个
字符串是给人看的），`Replayer::for_trace` 带进 `State`。**手上有 `Trace` 就用它** ——
走 `Replayer::new(&t.run)` 会拿到 0，而今天全部语料都是 A1/A2，看不出差别。

### 这张表是**认**出来的，不是抄出来的

[源码] 那边是属性名（`ButtDamage`），内核这边是 `ops` 里的一个数，中间没有词典。
生成器按 **(种类, 低进阶值)** 去认 —— 低进阶值就是内核今天写着的那个数
（A1/A2 实测钉的）。**候选个数和 [源码] 的用点数对不上就整条跳过并报出来**，
欠的那些在 `data/ascension.json` 的 `unmatched` 里逐条写着卡在哪。

> **正因为是"认"的，守卫测试才是这套东西的地基。**
> `asc_ops_low_value_still_matches_the_kernel_op` 第一次跑就抓到生成器把 op 下标
> 数成了"在它认得的那几种 op 里排第几"（活雾「膨胀」夹着一个 `EOp::Summon`）——
> **下标平移不会报错**，只会在高进阶下改掉另一个 op。
> 五条守卫见 verification-log 的 09-08 下午那条，其中
> `ascension_9_actually_raises_the_damage_that_lands` 是**正面证据** ——
> 前四条对一张全是 no-op 的空表也会全绿。

**A8 以上一次都没被观测碰过**（全部实录是 A1/A2），所以整张表是 `[源码]` 档，
不是 `[实测]` 档。对应的安全性质就是那条 `asc < 8` 提前 return。

---

## L2：单回合求解器（`src/solver.rs`）

```rust
pub struct Threat {
    pub incoming: [(i32, i32); MAX_ENEMIES],   // (每次伤害, 次数)
    pub live: bool,                            // 见下面「两种口径」
}
pub fn solve_turn(s: &State, threat: &Threat, score: fn(&State) -> i32) -> Line;
```

`Threat` 由**调用方**提供，L2 自己不预测敌人：

| 提供者 | 什么时候用 |
|---|---|
| 观测到的意图标签 | 对拍；以及**敌人认不出来**时的实战兜底 |
| 对齐到的招式的**面板基础值** | 实战驱动的默认口径 |
| `EnemyDef` 预测 | 跨回合 rollout / planner |

跨回合就是同一个 `solve_turn` 套在循环里换个威胁提供者。
**所以单回合求解器不是一次性的，它是多回合的内层循环。**

### `Threat` 的两种口径

| `live` | `incoming` 装什么 | 叶子怎么结算 |
|---|---|---|
| `false` | **最终值**（意图标签，已实测含双方乘区） | 照打，不过任何乘区 |
| `true` | **面板基础值** | 结算那一刻 `base + 当时力量` 过一遍完整伤害管线 |

**这不是精度开关，是"能不能看见一类决策"的开关。** `live == false` 下，凡是我这
一手会改敌人这一击的东西**全是空操作**：给敌人上虚弱、凌虐减力量、巨像减半、
转身改朝向、我自己吃污染。

升级在 `bin/solve.rs::upgrade_threat_live`，**三条前提缺一不可**：每一只有攻击意图
的敌人都认得出且对齐了 · 那一手里真有 `EOp::Attack` · **拿基础值现算一遍必须和
观测标签逐字相同**。最后这条是当场可证伪的自检 —— 对不上就整份退回标签并印出原因。
**不许一半现算一半照打。**

> **对拍那条路一个字没动**：`verify` 的契约仍然是"敌人打多少由观测注入"。
> 现算等于让内核自己预测敌人，那是"内核和内核比"。

### 打分发生在**挨完打之后**，这是刻意的

叶子不是"这回合打完的样子"，是敌人打完、下回合开始时的样子。于是：

* **格挡按它真正挡下的伤害计价，不按面板值。** 12 点格挡挡 8 点攻击只值 8 点，
  一点不挨打的回合里值 0。任何"格挡=好"的启发式都会在这里犯错。
* 回合末/回合初的结算（覆甲、绯红披风、灼伤、滚石、状态衰减）自动算进去，
  不用在 L2 重写一遍。

### 并列判据：**分数 > 打掉的敌人血 > 线更短**

原来是「分数相同取更短的线」，理由写着「少一次未建模的交互，而且手牌留着下回合
还能用」。**后半句是错的** —— 手牌回合末就弃掉，内核自己就是这么建的。于是并列时
它会主动扔掉真实伤害（骇鳗第 5 回合实测：`打击->打击` 和 `打击` 的 D=2 估值逐字
相等，短的那条赢，白扔 6 点伤害；**D=3 一模一样，加深度救不了**）。

`solver::consider_stopping_here` 和 `plan::plan_line_with_threat` **必须同口径** ——
不然 D=1 和 D≥2 会在并列局面上分岔，`plan_depth_one_is_exactly_solve_turn` 守着这件事。

### 目标函数的权重是**判断**，不是实测

`Weights` 里每个数字都写了理由。基准是"我的 1 点 HP = 100"，敌人 1 点 HP = 30
（1:3.3，**有意偏防守** —— 玩家指出过我防御端偏弱，上一局正是死在玻璃大炮上）。

现成的有三个：`survive_first`（默认）/ `damage_first`（竞速）/ `hp_only`。
**三个给出同一条线时，这条线的可信度比任何权重讨论都高**，`--live` 会自动报这个对照。

> **敌人血量那一项数的是 `content::remaining_hp_including_revives`，不是当前血量**
> —— 含还没出场的形态。会复活的敌人身上用当前血量会造出一个荒唐的激励：砍掉最后
> 几点让这一项从 4 跳到 200，于是求解器**宁可站着挨打也不收人头**（第 3 幕 Boss
> 实测到这一幕）。**改 `eval` 的时候别把它改回 `en.hp`。**

### `Weights::clock`：即死倒计时，**进所有目标函数**

沙坑（第 2 幕 Boss）是内核里唯一一条**不看血量**的死法：每个敌人回合减 1，
归零直接被吞。层数 = 「这条线还剩几个我的回合」。

```
clock_value(s) = − max(0, horizon(s) − 沙坑层数) × min(每回合伤害, 血墙) × w.enemy_hp
```

**一个新常数都没发明** —— `horizon` / `optimistic_damage` / `enemy_wall_all_forms` /
`w.enemy_hp` 全是现成的，币值和 `eval` 里敌人血那一项逐字相同：
少一个回合，就是少打一个回合的伤害，就是那么多血留在场上没啃掉。

**它和 `Weights::power` 的规矩不一样，故意的**：`power` 是软的跨回合收益，
塞进单回合目标函数会改掉驾驶路径，所以只在 `LEAF`；沙坑是**一条已经写死的
死刑判决**，和 `win`/`death` 一样从叶局面本身读得出来，而单回合目标函数
本来就允许（必须）看见死亡。风险面是空的：**全内容表只有一只敌人挂得出沙坑**。
`HP_ONLY` 留 0 —— 那把尺子的价值就是什么都不加权。

三处刻意的保守（方向一致：报出来的缺口只会比真实的小）：`horizon` 夹在
`HORIZON_CAP = 8` 而这场仗真要 9–11 个回合 · 牌堆里还没抽到的狂乱逃离不计入 ·
`optimistic_damage` 本来就偏大。

**刻度分不开**：`bin/bestline --sweep`（256 个种子，单回合策略推到底）——
`clock=0` 赢 7/256，`clock=25..200` 一律 22–25/256。**这一项的存在值 3.3 倍赢面，
它的刻度值不了什么**，所以取 100（公式的理论值），不假装它是标定出来的。

`Weights::LEAF` 是**跨回合搜索的叶评估**，和线的目标函数是两个参数
（标定说不该绑在一起）：`enemy_hp/hp = 0.75`，2026-08-25 用 `bin/calib --siblings`
标出来的，30 折交叉验证里 27 折一致。`Weights::power` 也只有 `LEAF` 非零 ——
给单回合目标函数加跨回合项会直接改掉驾驶路径。

### 药水：机会成本在搜索**外面**

药水不要能量，所以只要边际收益 > 0 求解器就会喝 —— 实测过：满血 80/80、第 1 回合、
一只打 8 点的敌人，它把三瓶**一次全喝光**。

定价**不进目标函数**。塞进 `eval` 有三个后果：数字消失（只剩一个分数，看不到
"这瓶省了 12 血、门槛是 10"）、污染验收器那条"实战线赢不过穷尽搜索"的自检、
把一个拍脑袋的价格混进已经对拍验证过的规则里。

做法是解 n+1 次（`solve_turn_potions` 带槽位掩码）：先解一条**一瓶都不喝**的基准线，
再逐瓶各解一次，差值就是"这瓶省几点血"。门槛在搜索外面比（`PotionPolicy`，
家规 `reserve_hp: 10`；`last_fight` 为真时归零）。

五条规矩：

* **定价用纯血量差**，不折算打掉的敌人血 —— 那部分价值跨回合兑现，L2 看不见。
  打死敌人省下的那一手伤害**已经**含在里面了（威胁按敌人死没死结算）。
* **不喝会死时门槛不起作用。** 不需要特判：死亡在目标函数里就压倒一切。
* **跨回合药水拒绝评分**（力量/敏捷/再生/鱼油/铁心）。它们的价值在后面几个回合，
  单回合视角下力量药水只值 1 点出头的血，而一场五回合的仗它可能值 20 点伤害。
  给一个自信的低分比不给分更危险。
  **判据是"价值在不在这一回合之内"，不是"给不给 status"**：屈伸药剂也给 status，
  但那是 `TempStrength`、回合末就还回去了 —— 它**照常定价**。
* **改「敌人这一手打多少」的药水（虚弱）在 `live == false` 下也拒绝评分**，
  原因不同：那时 `Threat` 是冻住的常数，给敌人上虚弱是空操作，定价必然得 0。
  **那个 0 是"看不见"不是"没用"。**
* **「身上几瓶」数的是真的几瓶，不是内核认得几瓶。** 拒绝定价是对的，从瓶数里
  抹掉是错的 —— 满仓不满仓是个真决策。
  一度加过"满仓时降价"，**试出来是错的**：3/3 打三折后门槛掉到 3 点血，于是满血
  第 1 回合又开始为省 3 点血喝掉格挡药水。满仓该不该清仓取决于"这一幕还会不会再
  掉药水"，那是 L3 的知识，L2 编不出来。

---

## 跨回合搜索

两套东西，别混：**rollout** 是把一场仗推到底（验收工具 + 叶评估的可选项），
**planner** 是限深 expectimax（`src/plan.rs`）。

### rollout 的两条策略（`rollout::Policy`）

| | 一回合的代价 | 谁在用 |
|---|---|---|
| `Fast` | 几十步的手写启发式 | `score::mcts_rollout`（长在 `solve_turn` 的叶子上，一个回合调约 20 万次，再套一层搜索是平方级）· P2 的方向判据 |
| `Solver { budget, score }` | 一次 `solve_turn`（默认 2000 节点） | 默认策略：`bin/rollout` 的验收，以及任何独立推演 |

求解器策略的三个约定，每条都有理由：

* **威胁由 `rollout::predicted_threat` 提供**，它要过一遍 `damage::apply_modifiers`
  —— `Threat` 的契约是**最终值**。`fast_play_turn` 那个粗估只做 `base + 力量`，
  我方带易伤时低估 50%。
* **回合边界照旧走 `step(EndTurn)`**（让 `EnemyDef` 真的打），预测的威胁只喂给搜索
  的叶子。机器面和策略面因此仍然分得开。
* **不许碰药水**（`allowed_potions = 0`）：边际收益 > 0 就喝，一场 30 回合的推演会
  在第 1 回合把整包喝光；而且 `fast_play_turn` 本来也不喝，两条策略要可比。

**目标函数必须用 `damage_first`。** 第一版用 `survive_first`，P4 当场 52/8320 ——
推演一条都没死，就是不赢：单回合看挡下 1 点永远比打出 1 点值钱 3.3 倍，迭代下去
就是只挡不打。**这是"用单回合目标函数当跨回合策略"的结构性后果**，不是权重没调好。

**闭掉子选择是策略的义务。** `step` 在 `combat_over` 之后对任何动作都原样返回，
所以"打出最后一张牌的同时立了个标记"会让闭环那个 while **挂死**（不是变慢）。
分辨"挂死"和"仗真的打不完"只有一条判据：**抬高回合上限，后者的数字会变，前者不会**。

### planner（`src/plan.rs`）：D=2 跑通了，**仍然不当驾驶员**

限深 expectimax。地基：抽牌堆的「已知前缀」`n_draw_known`（头槌那类放到顶上的牌是
**确定**的，不该当随机）· `pop_draw_top` 收口 · 回合边界拆成
`end_turn_before_draw` + `open_hand`（机会节点插在中间）· 机会节点 `chance_children`
· 置换表 `Tt`。**门是 `D=1` 必须逐字等于 `solve_turn`**，一个测试钉着。

默认配置（`Plan::default()`）：

```
depth 1 · widths [96,12,4,4] · exact_threshold 128 · budgets [200000,2000,500,500]
score damage_first · cand_score score::leaf · deep_score score::leaf
window_score score::window（窄根上接管 deep_score 和 leaf）
k 6 · k_certain 64 · power_reserve 2 · damage_reserve 2 · tt_bits 16
leaf Leaf::Eval(score::leaf) · ext_lethal true · ext_spike false · ext_boundary false
window 6
```

每个参数背后各有一条量出来的结论：

* **四个目标函数，默认里有两个是同一个、第四个只在窄根上出场。** `score` 从 2026-09-05 起**只管
  `depth <= 1` 那条提前 return**（和候选集建不出来时的兜底）；根候选排序归
  `cand_score`，深层每回合选线归 `deep_score`，两个默认都是 `score::leaf` ——
  **和 `cfg.leaf` 同一个函数**。
  `deep_score` 的理由是 expectimax 的规矩：决策节点取"后继子树价值"的最大值，
  而最后一层的后继就是叶子。2026-09-04 之前这里用的是 `cfg.score`
  （`damage_first`），于是最后一层的 argmax 取的是**另一个函数**的最大值。
  `cand_score` 是同一条道理往前推一层：**筛子和尺子必须是同一把**，
  见下面「候选生成」。
  第四个是 `window_score`（`score::window` = `Weights::WINDOW`，只和 `LEAF`
  差一个 `win = 0`）：**窄根上它同时接管 `deep_score` 和 `leaf`**，
  见下面「窗口内的目标函数」。挑法收口在 `Plan::at_root` 一个函数里 ——
  **一次搜索一把尺子，逐叶子挑是错的**。
  改它们**必须同时看 P4 截断率**（`leaf` 是 100:75，比 `damage_first` 的 40:60
  更防守，而 `survive_first` 100:30 当跨回合策略时 P4 当场 52/8320「只挡不打」）。
  A/B 用 `bin/rollout --alt "deep-score=damage"` / `--alt "cand-score=damage"`。
* **`window`（确定性窗口）是第三笔深度预算，和 `depth` / `ext` 互不相干。**
  这一层**一个骰子都不掷**时（`plan::window_is_certain`），它**不扣 `left`**，
  扣一笔独立的 `free`（默认 6 层）。理由是两种层的价钱差一个数量级：
  不确定的层要 `widths[d]` 个孩子（指数），确定的层只有一个孩子（线性）。
  而 `draw_pile_order` 补丁之后 `sync` 出来的局面**整堆牌序都是已知的** ——
  洗牌之前那一整段本来就是有限确定性博弈，不搜是白扔掉 mod 递过来的信息。
  **判据是「双重确定」不是「单孩子」**，见下面「确定性窗口」。
  `--alt "window=off"` / `bin/solve --window 0` 退回 2026-09-05 之前。
* **`k` 是决定这东西跑不跑得动的那个参数** —— 每多一条候选就多 `widths[1]` 次深层
  单回合搜索。**但那个乘数在窄根上是 1**，所以 K 是自适应的：
  `k_certain`（64）在 `plan::root_is_narrow`（牌序整堆已知）成立时接管，
  其余仍是 `k`。判据只有抽牌那一半，**敌人的随机分支不进来** ——
  它不产生分支，只是一条轨迹。详见下面「候选生成」。
* **`tt_bits`（置换表）是跨候选 memo，2026-09-05 才第一次真的接上。**
  key 是 `plan::key` 而不是 `solver::key`（后者缺 `n_draw_known` /
  `enemy_hist` / `enemy_used` / 三条随机流 / 消耗堆内容），
  搜索上下文（`depth`/`left`/`ext`/`free`）也揉进 key ⇒ 复用是**精确复用**。
  `0` = 不建表。**命中率是它唯一的证据**，`plan_audit` 和 `bin/solve --plan` 都印。
* **`exact_threshold` 是实测出来的，别凭直觉调大。** 第一版凭"精确更好"设 1024
  （而采样宽度只有 96），验收从 19.5 秒涨到 55.4 秒，**P5 的读数一个字没变**。
  判据：**精确只在它比采样还便宜的时候才划算。**
* **三条延伸只有斩杀（`ext_lethal`）划算**（8320 对配对样本：都不开 +2.4 /
  只斩杀 **+2.8** / 只尖峰 +2.3 / 只边界 +2.4）。判据是一句话：**延伸只有在能把
  "估值"变成"事实"时才划算** —— 斩杀那一层过后敌人死没死由 `combat_over` 说了算，
  估值里最大的一块不确定当场塌缩；尖峰和边界什么都没变成事实。

### 机会节点分三条路，别再合并

`chance_children` 按「这一手到底有多确定」分岔，三条各有各的判据：

| 情形 | 给什么 | 为什么 |
|---|---|---|
| 前 5 张全是已知前缀（`r == 0`） | 一个 `p = 1.0` 的孩子 | 身份全明确，**真的**确定 |
| 抽牌堆不够 5 张、**弃牌堆空** | 一个 `p = 1.0` 的孩子 | 剩下几张全抽走，没别的可能 |
| 抽牌堆不够 5 张、弃牌堆非空 | `width(depth)` 个样本，各 `p = 1/w` | **洗弃牌堆是真随机**，而且是那一刻的主导不确定性 |
| 未知区不超过 `exact_threshold` | 超几何枚举 | 精确比采样还便宜 |
| 其余 | `width(depth)` 个样本 | —— |

> **头两条和第三条 2026-09-02 之前是合在一个 `if` 里的**，一起返回单个
> `p = 1.0` 的孩子。那条路径不但不读 `Plan::seed`（于是可重复性扫描在
> **25% 的回合边界**上失明），返回的那一手**逐字节就是真实的下一手** ——
> 它拿到的局面正是 `step(EndTurn)` 马上要 `open_hand` 的那个。
> **planner 在 25% 的回合边界上带着答案在搜。**
> 三条守卫钉着，其中一条就叫 `the_chance_node_does_not_peek_at_the_real_next_hand`。

采样分支收口在 `sample_children`，**播种规则只有一处定义**。它有一个坑：
`shuffle_unknown` 只动抽牌堆的未知区，而洗弃牌堆用的是**状态自带**的
`rng.shuffle` —— 不把它也从样本流里播一次，w 个样本会逐字相同。
**而且只在真要洗牌时动它**，否则就顺手改掉了原有那条采样路径的深层行为。

### 确定性窗口：**这一层不掷骰子就不算深度**

`Plan::window`（默认 6）。判据是 `plan::window_is_certain`，**两半缺一不可**：

| 半 | 谁答 | 漏了会怎样 |
|---|---|---|
| 机会节点确定 | `plan::chance_is_certain` | —— |
| 每只**活**敌人的下一手唯一 | `step::allowed_next` 是单元素集合 | 带随机分支的敌人被当成确定，往下深搜是在**放大 `pick_next` 掷出来的一条轨迹**。这批局面有多少，`plan_audit` 自己报（`dropped_random`：牌序整堆已知、但敌人要掷骰的起点）|

`chance_is_certain` 比"孩子只有一个"**窄一点，这是故意的**：枚举分支在退化情况下
（未知区所有牌身份相同）也给单个 `p = 1.0` 的孩子，但那种"确定"只确定到 `plan::ident`
为止，而**`ident` 今天不含附魔的数值那半**（`ench` / `ench_amt`）——
带灵巧的防御和普通防御会并成一组。
拿一个已知欠定的身份口径去支撑"这一层是事实"，方向是乐观的。

三笔深度预算互不相干：`left`（计划好的、会分岔的层）· `ext`（延伸借的层）·
`free`（确定层）。`HARD_DEPTH_CAP` 原样保留兜底，`2 + 2 + 6 = 10 < 12`。

> **「确定层不扣 `left`」捆了两件事，读它的读数时要拆开**：
> ①把确定的那几层搜掉（免费，不引入任何新随机）；
> ②把计划好的那个**分支层推到窗口之后** —— 那一层原来根本没发生（`left` 被
> 确定层吃掉了），现在发生了，而且落在 `widths[2..]` = 4 这一档。
> 第 ② 条是耗时 +8% 和**单次可重复性 74% -> 71%** 的来源，
> 不是"深度买不到东西"。两个拆开量的实验见 verification-log。

> **它为什么该白送**：不确定的层要 `widths[d]` 个孩子（指数），确定的层只有一个孩子
> （线性）。而 `draw_pile_order` 补丁之后 `sync` 整堆牌序照抄 —— 洗牌之前那一整段
> 本来就是有限确定性博弈。**不搜它是在白扔 mod 递过来的信息**，
> 而且 `bin/rollout` 的采样只换 `rng.shuffle`/`rng.enemy` 两个种子、
> **不动抽牌堆数组**，所以窗口里"预测"的那几手正是推演真的会发的那几手 ——
> 这不是偷看，是那份信息本来就在手上。

### 两个血墙口径，**故意不统一**

| 函数 | 数什么 | 谁在用 |
|---|---|---|
| `solver::enemy_wall` | 只算**当前形态**（`hp + block`）| `plan::want_extension` 的斩杀判据 |
| `solver::enemy_wall_all_forms` | **含还没出场的形态**（`remaining_hp_including_revives + block`）| `solver::horizon` |

判据还是延伸那一条：**只有能把"估值"变成"事实"的东西才算数。**
过一层之后成为事实的是**「这一形态死没死」**，所以斩杀判据用当前形态；
而 `horizon` 答的是"这仗还要打几个回合"，后面的形态是必然要来的。

> 2026-09-02 之前 `horizon` 也读 `enemy_wall`，于是第 3 幕 Boss 复活那一帧
> 地平线从 **1 阶跃到 7**，所有正比于 `H` 的能力项当帧翻七倍。
> 反过来把斩杀判据也换成整场口径同样是错的：多形态 Boss 上斩杀延伸会**永不触发**，
> 那正好关掉了三条延伸里实测唯一划算的一条。两条各有一个守卫测试。

### 候选生成：筛子和尺子必须是同一把（2026-09-05，阶段 3）

根候选集是整条 planner 里**唯一一个下游修不动的瓶颈**：不在集合里的线，
`deep_score` 评得再准、`window` 搜得再深，都评不到。
`plan_audit` 的读数 1 专门量它，而阶段 1（深层目标函数）和阶段 2（确定性窗口）
两次端到端平局的诊断是同一句话 —— **到边界最优的那条根线有 17–21/45 不在候选集里**。

三件事，各自一个旋钮，可以单独关掉做 A/B：

| 旋钮 | 做什么 | 关掉 |
|---|---|---|
| `cand_score` | 剪枝的分数换成和深层/叶评估同口径的 `score::leaf` | `--alt "cand-score=damage"` |
| `k_certain` + `root_is_narrow` | **窄根**上把 K 从 6 开到 64 | `--alt "k-certain=off"` |
| `tt_bits` + `plan::key` | 几条候选共享一张置换表 | `--alt "tt=off"` |

**K 为什么可以只在窄根上开大**：K 花的钱是每条候选一次深层求值，
而深层求值的价钱由机会节点的宽度定 —— 牌序整堆已知时 `chance_children`
给单个 `p = 1.0` 的孩子，一条候选往下就是**每层一次 `solve_turn`**（线性）；
否则是 `widths[d]` 个孩子（指数）。**同一个 K 在两种根上贵得不是一个量级。**

> **`root_is_narrow` 的判据只有抽牌那一半，这和「窗口的判据必须是双重确定」
> 不矛盾 —— 两者问的不是同一件事。** 窗口问"这一层是不是事实"（敌人掷骰就不是）；
> 这里问"这条候选往下要花多少钱"，而 `node_value_window` **只按机会节点的孩子
> 铺开**，敌人那一手是 `pick_next` 掷出来的一条轨迹，一个分支都不产生。
> 于是 `root_is_narrow` 比审计台筛确定性起点那两条**宽**，宽出来的正是
> 「抽牌定死了、敌人要掷骰」那 14 个 —— 那批局面里 K 开大照样便宜。

> **要的是"整堆已知"（`n_draw_known == n_draw`），不是 `chance_is_certain` 的
> "够盖住一手"**：整堆已知这条性质**自己会往下传**（打牌只把牌挪去弃牌堆/消耗堆，
> 抽走的从已知前缀里出），一路传到洗牌边界；只已知 5 张的话，根这一手随便抽两张就破了。

**置换表接上之前必须另写 `plan::key`。** `solver::key` 是给**单回合**去重用的，
它省掉的正是回合内不变、跨回合会变的东西：`n_draw_known` · `enemy_hist` /
`enemy_used`（25 条 `NotTwice` / 3 条 `Once` / 3 条 `AtMost` / 3 条冷却都读它）·
三条随机流 · 消耗堆的**内容**（只记了 `n_exh`）· `turn` / `player_dead`。
回合内这些一个都不变，所以那边是对的，而且**不该为跨回合多花钱**
（它每个搜索节点都算一次）。拿它做跨回合 memo 是个**静默错误** ——
四种对拍模式全看不见。搜索上下文（`depth`/`left`/`ext`/`free`）也进 key，
所以复用是**精确复用**，`TtEntry::depth >= need` 那条只剩兜底。
表是**无锁**的（两个 `AtomicU64` 异或校验配对），因为候选是并行求值的，
而跨候选 memo 的全部价值就在于那几个线程看的是同一张表。

### 窗口内的目标函数：拿掉那个值 1000 血的 `win`（2026-09-05，阶段 4）

`Weights::WINDOW` = `Weights::LEAF` 但 **`win = 0`**，在**窄根**上同时接管
`Plan::deep_score`（深层选线）和 `Plan::leaf`（叶评估）。
旋钮 `window-score`，`window-score=leaf` **逐字节退回阶段 3**。

**赢的价钱本来就写在 `eval` 里了。** 它在 `combat_over` 那条提前 return 之前
不数敌人血量，所以赢下来的局面自动省掉 `Σ 敌人剩余血 × 75` 这一整项 ——
赢值多少 = "不用再啃的那些血"，和函数里其余每一项同一套币值。
`win = 100_000` 是**额外**再加的一笔，等于 **1000 点血**：窗口里只要一条线
在洗牌边界之前把仗打完，它就以 1000 血压过所有没打完的线，**不管两边各剩多少血**。
而"仗有没有在窗口内结束"取决于窗口有多长，是个**地平线人造物**。
窗口里一个骰子都不掷，所以仗真的在那一段里打完时 `hp × 100` 就是这场仗的
**最终血量** —— 正是 P5 端到端量的那个数，不是它的代理量。

> **尺子必须每次搜索一把，逐叶子挑是错的 —— 第一版就是那么写的。**
> 两把尺子只在"赢下来的终局"上差一个常数，混着用会出现这一幕：
> 候选 A 这一回合就把人打死（确定，`win = 0`），候选 B 没打死、
> 但它**窗口外的某个采样分支**赢了（`win = 100000`）。
> **A 的事实输给 B 的运气，差 1000 血 —— 偏置方向正好是反的。**
> 所以挑法收口在 `Plan::at_root`，判据取 `root_is_narrow`
> （和 `k_certain` 同一条：整堆已知这条性质自己会往下传，一路传到洗牌边界）。
> 敌人掷骰不进这个判据 —— 它只会让**窗口**提前关掉、退回采样，
> 尺子仍然是一次搜索一把。

**`cand_score` 故意不换**：换了 `plan_audit` 的读数 1（候选覆盖）就不再对
目标函数免疫，而那一栏的全部价值就在于它免疫。

**审计台跟着换了一半**：`plan_audit` 的 A2 那一列答的是"planner 会挑哪条根线"，
所以它用 `Plan::at_root` + `Leaf::score_fn` 拿 planner 真的在用的那把尺子。
**裁判没动** —— A0/A/B/C 四列的叶子仍然一律 `score::leaf`，后悔仍然一律在
C 那把尺子上量。

**读数 3 的两列要按这条分界读**（报告里也印着）：裁判自己带着 `win = 100000`，
所以**不含 win 那一列是口径中立的**（两条线要么都赢要么都不赢，两把尺子给的排序
逐字相同），可以直接和阶段 3 比；**含 win 那一列是两把尺子打架的地方**，
它涨了不等于变差，真正的裁判是 P5 和 P4。
守着这条分界的是 `the_two_rulers_differ_only_on_a_won_terminal`。

### 能力牌：**卡死的是候选生成，不是叶评估**

`k` 条候选是按**单回合分数**剪的，而能力牌当回合的产出定义上就是 0 伤害 0 格挡 ——
打出薪火之源的线必然垫底，**在根上就被挤出候选集**，深层搜索和叶评估根本见不到它。

> **推论：只修叶评估是死路** —— 修好的叶子评不到一条不存在的候选线。

所以 `power_reserve`（默认 2）给**打出过能力牌的线**在 `k` 之外额外留几个名额。
判据读 `CardDef.kind` 这张数据表，不认牌名（不变量 3）。**保底是纯增量的**：
只补没能挤进 top-K 的能力线，一条原有候选都不挤掉、顺序也不动，
`power_reserve_only_adds_candidates_never_reorders` 守着。`--power-reserve 0`
和加它之前逐字节相同。

> **2026-09-05 补一句**：`cand_score` 换成 `score::leaf` 之后，这两条保底
> （能力线 / 高伤线）补的其实是**同一个错的两个特例** —— 它们各自"被另一把尺子
> 判低了"。同一把尺子之后它们大部分本来就进得了 top-K。
> **保底没有撤**：它仍然是纯增量的、成本已知的，而"大部分"不是"全部"。

叶评估这一半也补了（`Weights::power` + `solver::power_horizon_value`），但**只计价
三样**，按上面那两种结构分开写：

```
P(s) = ΔE · dpe(s) · w.enemy_hp · H                       ← 额外最大能量（薪火之源）
     + ramp · H(H+1)/2 · w.strength                       ← 每回合 +力量（恶魔形态）
     + Σ_{活着的敌人 e} min(boulder·Hₑ + 5·Hₑ(Hₑ−1)/2, Rₑ) · w.enemy_hp   ← 群伤（滚石）

Rₑ = content::remaining_hp_including_revives(e)           ← 和 `eval` 同口径
H  = clamp(⌈enemy_wall_all_forms / optimistic_damage⌉, 1, HORIZON_CAP=8)
Hₑ = clamp(⌈(Rₑ + e 的格挡)   / optimistic_damage⌉, 1, HORIZON_CAP)
```

**滚石那一项逐只封顶、地平线也逐只算**，两件事缺一不可 —— 少任何一件，
「收人头」在叶评估里就是**负收益**（实测过 −16425 和 −1125 两种）。
封顶之后有一条可证明的不变量：收掉剩 R 血的敌人，`eval` 白赚 `R·w.enemy_hp`，
而这一项最多跌 `min(总伤害, R)·w.enemy_hp ≤ R·w.enemy_hp` ⇒ **净收益恒 ≥ 0**。
`killing_an_enemy_never_lowers_the_leaf_score` 和
`boulder_horizon_is_measured_per_enemy` 两条守着。

> **前两项还欠着同一个毛病的小一号版本**：它们正比于整场的 `H`，而 `H` 是
> `ceil` 的阶梯函数 —— 收掉 1 点血跨过一个边界，`H` 掉一整档。
> 实测恶魔形态最多亏 −525、薪火之源 −825（滚石修好之前是 −16425）。
> 修它要动 `horizon` 本身，是模型改动不是 bug 修复，**故意留空**，
> 三条候选路各自的代价见
> [verification-log](docs/verification-log.md#还欠着的一半前两项仍然会随-h-阶跃倒挂)。

**`H` 用的是 `enemy_wall_all_forms`（含还没出场的形态），不是 `enemy_wall`。**
后者只算当前形态，是 `plan::want_extension` 那条斩杀判据专用的 ——
**两个口径故意不同**，理由和实测的阶跃读数见下面「两个血墙口径」。

**明确拒绝计价三类**，判据是"这一项在叶子上是不是 determinate"：
**每回合给格挡的**（价值 = `min(格挡, 来袭)`，而 `fn(&State)->i32` 拿不到来袭 ——
按面板值算就违反了"格挡按真正挡下的伤害计价"）· 会衰减的 · **触发式的**
（每回合触发几次取决于抽到什么）。
**第一类是最大的缺口，而且正是当前这局的绯红披风构筑。**

### 为什么 D=2 仍然只当被测对象

两个数说的是两件事，**不冲突**：

| 量的是 | 怎么量 | 读数 |
|---|---|---|
| **平均**质量 | P5 配对比较（同种子 CRN） | D=2 比 D=1 均值 **+3.8 血**，赢面 6.9:1 |
| **单次**可重复性 | `tools/plan_seed_sweep.py`：同一个局面只换 `Plan::seed` | 294 个回合起点里 **69 个换种子就换线 = 23%**（单回合求解器是 **0**，回合内穷尽、确定） |

> 同一次扫描的第三个数：**只有 97/294 = 33% 的起点，D=2 在每个种子下都和单回合线
> 一致** —— 也就是说**三分之二的回合它在说不一样的话**，而其中 23% 连它自己换个
> 种子都不认。这两个数一起才是"当分歧样本读"的依据。
> （读数取自 2026-09-03 的 63 条语料；语料涨了要重跑。）

> **这两个数 2026-09-02 都变差了**（+4.0 -> +3.8，13% -> 22%），
> **那是修正不是回归**：抽牌堆凑不齐一手时机会节点原来塌缩成单样本，
> 而那个"样本"逐字节就是**真实的下一手** —— 25% 的回合边界上 planner 是带着
> 答案在搜，而且那条路径不读 `Plan::seed`，扫描把它们一律记成"稳定"。
> 拆开采样之后作弊分没了、看不见的不确定性也现形了。
> 全部证据在 [verification-log](docs/verification-log.md)。

**实战一个回合只发生一次，要的是后者。** 所以驾驶继续用单回合 `survive`，
`solve --live --plan` 只把 D=2 并排报出来当分歧样本。

`Leaf::Rollout`（`--leaf rollout:8:12`，叶子改成推几个回合看结果）**加固了这个结论
而不是推翻它**：它买到的正是上面拒绝计价的那三类（绯红披风每回合的格挡真的去挡了
那一手、撕裂真的被自伤唤醒、黑暗之拥真的因消耗而抽牌），**平均质量是最好的**
（同语料 A/B：P5 均值 **+0.4**，更好 +102 / 更差 −41 —— 那次量在 14144 对的旧语料上，
**别和上面那个 +3.8 直接比**），但**单次可重复性从 87% 掉到 71%**，而且加样本救不了
（32 样本只把不稳定从 72 个降到 64 个）—— 那不是采样方差，是推演叶子让很多候选
真的成了**近似平局**。所以它是可选项，不是默认。
（**那对 87%/71% 量在 2026-09-02 修机会节点之前**，基线现在是 78% —— 两个数
要重跑才能比。结论的方向不受影响：那次量的是同一份二进制上的 A/B。）

> **确定性窗口也往这个方向掉了 3 个百分点**（74% -> 71%），但性质不同：
> 那是「确定层不扣 `left`」把一个原本不存在的采样层推了出来，不是候选变成了
> 近似平局。机制和两个拆开量的实验写在 verification-log。
>
> 要让它能驾驶，该做的不是加样本（量过，没用），而是解决"近似平局"本身：
> 更强的 rollout 策略，或者在候选之间做**配对显著性判断**而不是取 argmax。
> **加深 D 也不在队列里**：D=3 也只兑现 2 次触发，追不上 5 回合的引擎，
> 成本指数级、种子不稳定性还会涨。

---

## L3：合成战斗构造器（`src/synth.rs`，阶段 1）

```rust
pub fn build(spec: &FightSpec) -> Built;   // Built { state, gaps }
```

`(牌组, 遗物, 血量, 遭遇, 种子) -> State`，出来的是**第 1 回合的起点**：
洗过牌、发过手牌、回合开始的钩子跑过了。L2 从这里直接接手。

**为什么需要它**：对拍那条路上每个局面都是从观测同步来的（`Replayer::sync`），
而 L3 问的是**还没打的仗** —— 没有观测可抄，必须凭牌组和遗物搭出来。

### 它自己不实现任何游戏规则

用的是 `State::new` + `add_card` + `add_enemy` + `step::grant_relic` +
`begin_combat`，外加两张数据表（`content::RELICS` / `asc::ASC_HP`）。
**一条 `if 牌名` / `if 遗物名` 都没有** —— 有的话就说明表里缺东西。

### 三样它**故意**不猜，每样都报出来

| 拿不到的 | 处置 | 为什么不猜 |
|---|---|---|
| 遗物的跨战斗计数器（摆动球的相位） | 用 0，**报 `Gap::RelicCounterMissing`** | 0 是个具体的相位，猜错就是整场早/晚一个回合抽牌 |
| 敌人初始血量 | 从 `asc::hp_range` 的区间里掷（**它的第一个生产消费者**） | 表里没有这只就退回 `EnemyDef::max_hp` 并报 `Gap::NoHpRange` |
| 内核不认识的牌/遗物 | 照放，逐条报 `Gap` | 「不认识」和「没效果」是两件事 |

**血量掷点走一条独立的随机流**（`synth::hp_stream`），不碰 `State::rng` 的三条。
理由和不变量 4 的分流是同一句：掷血量在开仗之前，让它扰动洗牌流就等于
"换一只敌人的血"会改掉整局的抽牌顺序，**CRN 配对比较当场失效** ——
而 L3 的候选之间正是靠 CRN 比的。

### `SYNTH_ONLY_GAPS`：`modelled` 那一列问的是**哪条路**

`RelicDef::modelled` 一直被读成"这件遗物建好了没有"。它其实是
**「对拍路径上够不够」**：那条路上手牌 / 血量 / 药水槽全是观测量，`sync` 每帧照抄，
内核再建一遍就是重复计数（风箱和小血瓶的 `note` 里写着这件事，都是踩过的）。

**合成路径没有观测可抄**，于是这批"靠观测兜底"的遗物在那边一件都不生效。
差集就是 `content::SYNTH_ONLY_GAPS`，每条写着**它在开局那一刻少做了什么**，
由 `build` 报成 `Gap::SynthUnmodelledRelic`。
**几条走「内容清单」那张表**（`count_content.py` 会数它），别在这里写死。

> **两条路两个覆盖率，这本身就是结论。** 同一件遗物可以「对拍够了」而
> 「合成不够」—— 只有一个 `modelled` 列的时候，这件事没有地方可写。

### `CONDITIONAL_START`：局外信息由**调用方**给

三件遗物的条件在**战斗观测里根本没有**：古茶具的「上一个房间是不是休息处」·
骨茶的「还剩几场」（[源码] `ShowCounter => false`，游戏连面板都不显示）·
缩放仪的「这一场是不是 Boss」。它们因此**不能进 `RelicDef::private_status`**
（那一栏是无条件挂的），走 `content::CONDITIONAL_START` + `FightSpec` 的三个输入
（`after_rest` / `RelicSpec::counter` / `boss_room`）。

> **「欠一个局外信息」有时候等于「欠一个参数」。** 这三件在 `modelled: false`
> 里挂了很久，理由写着"不在 L1 里"—— 对，但**在 L3 手上**：整幕链自己知道
> 在模拟哪个房间。2026-09-09 补的就是那个参数，不是机制。

**对拍路径一条都不挂**：那边能量/手牌/血量本来就是观测量，挂了就是重复计数
（风箱和小血瓶的 `note` 里记着这个坑）。

### `ENEMY_START_PLAYER_STATUS`：敌人给**玩家**挂的开局 status

`EnemyDef::start_status` 只装挂在它自己身上的那些，而 [源码] 里存在给对面挂的
（`Rocket.AfterAddedToRoom` 给玩家 `SurroundedPower`）。那一类走这张**按名字索引**
的侧表（今天 1 条），消费点是 `begin_combat`。

按名字不按下标：表增删不会漂，而拼错会被 `enemy_start_player_status_names_resolve`
抓住。**没给 `EnemyDef` 加一栏**是因为 73 条里 72 条是空的 ——
把一个稀疏事实摊成一列噪声。

### 遭遇表（`src/synth/encounters.rs`）

`data/encounters.json`（[源码] 幕→遭遇→怪物）+ `data/enemy_ids.json`
（[实录] 英文类名 ↔ 内核敌人）读进来，**只做查表和连接**，一条解析规则都没有。
类名 -> `kernel_enemy` -> `replay::enemy_id`（**名字解析只有那一处实现**，
`dump_encounters.py` 读的也是它）。

**它敢于放弃**：构成含随机的遭遇（`exact: false`）`resolve` 整条拒绝并说明原因 ——
编一个自洽的多重集出来是本仓库明令不做的事。认那一场仗时另有一条宽口径
（`identify` 的第二个返回值）：只要观测到的敌人全落在 `all_possible` 里就报
"构成含随机"，**和"唯一命中"分开报**。

### `from_obs`：观测 -> `FightSpec`，两个台子共用一份

`synth_audit` 和 `fight_eval` 都要拿实录的第 0 帧搭同一场仗。这一步生在
审计台里（它当时是唯一的消费者），2026-09-10 搬进 `src/synth/from_obs.rs` ——
两边各写一遍的话，两个台子读出来的**就不是同一场仗**，而"哪个台子抽错了"
没有任何东西会报。

两处「第 0 帧已经不是开局那一刻」的修正也在里面，**两个台子都要**：
`DECK_REWRITERS`（风箱/骨茶/碎石者开局就把牌升了，照喂进去会**再升一次**，
所以整件扣下并报出来）· `corrected_hp`（`FightSpec::hp` 的语义是「**进这场仗
之前**」，而观测里小血瓶那 2 点已经回过了）。

---

## L3：单场评估（`src/synth/eval.rs`，阶段 2）

```rust
pub fn evaluate(spec: &FightSpec, cfg: &EvalCfg) -> FightEval;   // N 次采样
pub fn paired_delta(a: &FightEval, b: &FightEval) -> Option<PairedDelta>;
```

阶段 1 让内核**搭得出**一场还没打的仗，这一层把它**打完 N 次**。
报的不是一个分数，是一份分布：死亡率 · 终点血量 · 战损 · 回合数 · **截断率**。

### 判什么：死亡率在前，血量在后，**两者不许混在一个数里**

旧 advisor 的毛病是 ΔHP **在死亡处被截断** —— 一场两边都会死的仗里每个候选的
ΔHP 都塌向 0，排名恰好在最要紧的时候失声。所以这里：

* 死亡是**一个单独的计数**，不是"掉了很多血"的极端值；
* 血量分布的分母**只有打完并活下来的样本**，它答的是"活下来的话大概剩多少"；
* `paired_delta` 同样拆开：**只有 A 死 / 只有 B 死**的对数单独数
  （那才是"更安全"的直接证据），血量差只在**两边都活下来**的对上算。
  第一版没拆，86% 死亡的那场 Boss 当场被算成"升级之后更差" ——
  死掉那一侧的 `final_hp` 是 0 或负数，平均进去就是**同一个老毛病换了个地方犯**。

### 截断是**硬判据**，不是一栏诊断

撞上回合上限的推演**战斗没有分出胜负**，而它照样带着一个血量。所以截断的样本
**既不进死亡数、也不进血量分布**，单独计数，而 `bin/fight_eval` 在截断 > 0 时
**退非零码** —— 这是 L3 台子把守的唯一一个硬门。

> **别用抬高 `--max-turns` 去关掉它。** `Outcome::enemy_hp_left` 就是用来分辨
> 「差一口气」（接近 0）和「僵住了」（几百血）的，而僵住通常意味着**策略**
> 选错了目标函数 —— 用单回合的 `survive_first` 当跨回合策略实测 52/8320 条
> 只挡不打。默认策略因此是 `Policy::default()`（求解器 + `damage_first`）。

### CRN：共享的是**起点**，不是序列

样本 `i` 的种子只由 `(spec.seed, i)` 决定，**牌组进不来**（`eval::sample_seed`）。
于是两个候选在样本 `i` 上看到的**敌人血量逐字相同** —— `synth::hp_stream`
只吃 `spec.seed`。

**但共享到此为止**：牌组一换，洗牌流和敌人流**消耗的次数**当场就不一样
（仗长一个回合，敌人就多掷一次），两条流从那一刻起各走各的。
所以配对之后**逐场仍然有噪声**，判据要看**一批仗上的方向**。
实测（`--upgrade-check`，基础打击/防御全升，三个种子基）：
**血量那一维 79 场里 69–73 场为正、均值 +2.3～+3.1**，方向稳；
**死亡那一维救回 93–99 / 反而死 36–60**，比值只有 1.6～2.4:1。
⇒ **死亡率是噪声最大的那一维，而它正是 L3 要判的东西**，
阶段 3 得按这条排采样预算。

### 每个样本各 `build` 一次

不是"搭一次然后换种子推演"：敌人血量是 `asc::hp_range` 掷出来的，
**40 血的小怪和 48 血的小怪不是同一场仗**，搭一次就把这条方差冻掉了。
代价是每个样本多一次 `build`（微秒级，79 场 × 64 样本共 1.1 秒）。

### 已知偏差**从这副牌组自己算**（`eval::Caveat`）

不是一段写死的免责文本 —— 那种东西会被读者跳过。四条各带着数：
牌组里几张能力牌（引擎牌评不到 ⇒ **低估**）· 身上几瓶药水（推演一瓶都不喝
⇒ **低估**）· 构造器报了几条缺口 · **A8 以上**（进阶数值整张表是 `[源码]` 档，
全部实录是 A1/A2）。没有能力牌的牌组不会看见第一行。

---

## L3：整幕链式评估（`src/synth/act.rs`，阶段 3）

```rust
pub fn evaluate_act(spec: &FightSpec, plan: &ActPlan, t: &Table, cfg: &ActCfg) -> ActEval;
pub fn paired_act_delta(a: &ActEval, b: &ActEval) -> Option<ActDelta>;
```

阶段 2 把**一场**仗打完 N 次，这一层把**一整幕**串起来走 N 次：
遭遇按 [源码] 的分布抽、**血量跨场累计**、死在第几间单独记。
路线图给 L3 定的判决（「走完这一幕的死亡概率」）到这里才问得出来。

### Boss 钉不钉：**局外已知的东西不该掷骰子**（2026-09-12，阶段 4）

`ActPlan::pin_boss` 给了就不掷。实战里这是个**已知量** —— 地图屏一进去就写着
这一幕的 Boss 是谁（mod 的 `map.boss.id`，[源码] `ActModel.BossEncounter`），
而 Boss 那一场是整幕死亡率里最大的一块：掷一个均匀的 Boss 等于把一条已知信息
换成方差。

**这是阶段 1 那句「欠一个局外信息有时候等于欠一个参数」的第四条**
（前三条是 `after_rest` / `RelicSpec::counter` / `boss_room`）。

两条守着它的性质，各有一个测试：

* **钉错幕的 Boss 整个拒绝作答**（`ActRefusal::NoSuchBoss`）。退回去掷一个的话，
  报出来的数会像是"按你说的那只算的"
* **钉了不许扰动这条链的其余部分**：抽 Boss 原来要掷一次骰子，跳过它却让后面的
  抽牌漂掉，就等于"钉 Boss"顺手换了整幕的遭遇 —— 而两个候选之间的 CRN
  正是靠那一维。（今天 Boss 是最后抽的，所以天然不漂；测试钉住这件事，
  免得以后往后面加抽什么。）

### 路线**不建**，由调用方给

`ActPlan::rooms` 是一串房间（`M` 杂兵 / `E` 精英 / `B` Boss / `R` 休息处），
「走哪条路、走几间、在哪歇」是局外决策，路线图第一刀就划在外面。
房间类型**只有这四种** —— 商店/事件/宝箱对战斗链是空操作，
写进来只会让人以为建了。
`bin/act_eval` 的默认路线是一个 **`[判断]`**（8 杂兵 + 1 精英 + 3 休息 + Boss），
**打印在报告最上面**，`--rooms` 随时换掉。
**不要从语料条数去数「这一幕打几场」** —— 录制是抽查的（家规：精英+Boss 必录），
trace 数系统性小于真实战斗数。

### 抽序列照 [源码] `ActModel.GenerateRooms` 的**分布**

前 `NumberOfWeakEncounters` 场从弱怪池抽、其余抽到 `BaseNumberOfRooms` 场、
精英预抽 15 场、Boss 均匀取一个（A10 的第二个 Boss 在其余里再取一个，
**只有最后一幕**）。`GrabBag` 权重全是 1 ⇒ 等价于「在满足谓词的那些里等概率抽、
抽完拿走」；`AddWithoutRepeatingTags` 不许和上一场共享 tag。
**随机流本来就不和游戏一致**（不变量 4），这里要的是分布对。

> **弱怪和普通怪进的是同一个输出列表**，所以普通池抽的第一场要和
> **最后一场弱怪**比 tag。分成两个列表的话，「连着两场史莱姆」
> 会在唯一需要这条约束的地方漏掉。

### 开不出来的那一场：**跳过并计数**，不是不存在

抽序列**故意不按"内核认不认识"过滤** —— 过滤掉就等于
"这一幕只有内核认识的那几场"，那是个静默的谎。抽到开不出的就
**这一间不打、计一笔 `ActSample::unsimulated`**，血量原样带下去。
方向因此是**乐观**的（少挨了几场打），所以 `ActEval::coverage` 和
`unsimulated` **必须和结论并排印**。今天平均每条链漏 2.5 场。

### 休息处：[源码] 0.3×上限，向下取整

`HealRestSiteOption.GetBaseHealAmount = MaxHp * 0.3m`，落地那一步
`SetCurrentHpInternal` 是 `(int)Math.Min(hp + amount, MaxHp)`。
73 血上限回 **21**（不是 22）。`Hook.ModifyRestSiteHealAmount`（有遗物改这个数）
**没建**，方向是低估自己。

### CRN：比单场**多共享一维**，而那一维买下了整个判决

阶段 2 量到「死亡率是噪声最大的那一维」，并把方差缩减留给了这一层。
多共享的正是路线图点名的那个：**「这一幕抽到哪几场遭遇」**
（`seq_seed` 只吃 `(base, i)`）＋**逐场敌人血量**（`fight_seed` 只吃 `(base, i, k)`）。

**它值多少，量过**（`--upgrade-check`，基础牌全升，78 副牌组 × 64 条链）：

| | 救回来的链 / 反而死掉的 | 比值 |
|---|---|---|
| CRN 开（默认） | 529 / 66 | **8.0 : 1** |
| `--crn-off`（B 臂换一条随机流） | 1138 / 712 | 1.6 : 1 |
| 单场版本（`fight_eval --upgrade-check`） | 93–99 / 36–60 | 1.6–2.4 : 1 |

> **关掉 CRN，整幕的死亡那一维当场掉回单场的水平。**
> 所以这个判决是**方差缩减**买来的，不是"一条链里有好几场仗、信号更多"
> 买来的 —— `ActCfg::crn_salt` 那个旋钮存在的唯一理由就是把这两件事分开。
> **阶段 2 那个问题（加样本还是方差缩减）的答案是后者。**

### 两个配对入口：换牌组的和换路线的

`paired_act_delta` 要求**同一条路线**（比牌组的时候路线必须固定，换了就不是
同一个问题了）。但「走不走精英」本身就是一个决策，而它**只能**由两条不同的
`rooms` 表达 —— 所以那一对走 `paired_act_delta_across_routes`
（前者查完路线就调它，逻辑只有一份）。

**两个入口共享的 CRN 不一样，报告里要说**：换路线那一对仍然共享
「这一幕抽到哪几场遭遇」（`seq_seed` 只吃 `(base, i)`），但**逐场敌人血量
那一维在路线错位的那一刻就分岔了**（`fight_seed` 吃房间下标 `k`）。
所以它比换牌组那一对噪声大，`bin/advise` 会在那一行标出来。

### 截断：整条链作废

一场仗撞上回合上限**没有分出胜负**，而它照样带着一个血量；带进下一场，
后面每一场都建在假前提上。所以链停在那里，整个样本既不进死亡数也不进
血量分布 —— 和 `eval` 对单场的处置是同一条，`bin/act_eval` 守那道硬门。

### 回溯检验：**它判的是偏的方向，不是内核对不对**

路线图给阶段 3 定的独占判据。每条实录是一个 `(真牌组, 真遭遇, 真战损)` 样本，
拿预测的战损分布去包它。`fight_eval` 当初拒绝做这件事的两个理由今天仍然成立，
所以是**带着它们读**：

* trace 末帧不保证是战斗结束 ⇒ **只认末帧落在战利品/选牌界面且场上没有敌人**的那些
  （78 条里 71 条够格，其余逐条报"为什么不算"）；
* 实测那一场是**玩家**打的、这里跑的是**我们这套策略** ⇒ 覆盖率是
  「玩家 + 策略」两个差的**合量**，读成"内核准不准"就错了。

**那它判得了什么：偏的方向和大小。** 今天的读数是
**预测每场比实测多掉 6.9 血**，分位数排名直方图重重地堆在左边
（26/70 落在最低那一档）—— 我们这套策略比玩家费血。
**整幕链的结论正比于这个偏差乘以场数**：一幕打 5 场就是 35 点血，
今天 63% 的整幕死亡率要带着这一条读。

---

## L3：MCP 接线（`src/bin/advise.rs` + `tools/advise_core.py`，阶段 4）

```text
游戏 ──HTTP──► tools/advise_core.py ──JSON/stdin──► target/release/advise ──► 一份人读的报告
               （脏活：读状态、认牌名、挑候选）      （L3：evaluate_act / evaluate + 配对比较）
                                                            ▲
                        advisor/server.py（八个 MCP 工具，只做映射）
```

和 `solve_now.py -> solve --live` **同一条路**（独立可执行文件 + JSON/stdio），
理由在「构建与环境」：这台机器是 GNU 工具链，PyO3 过不去，而边界调用本来就少。

### 一条命令答四个决策，靠的是「候选」这一个概念

拿牌 / 移除 / 升级 / 走不走精英，在 L3 眼里是同一件事：
**一副基准牌组 + 若干个改动，在同一批随机上配对比**。所以请求里只有一种东西：

```json
{"label": "拿「铁甲」", "add": [{"name": "铁甲"}], "remove": [3], "upgrade": [5], "rooms": "EMRB"}
```

**候选按下标寻址**（不是按牌名）：三张打击里带附魔的是哪一张，名字答不了，
而挑哪一张是调用方的事。**内核只认下标**，名字只进标签。

三个问法共用这一份请求：`act`（走完这一幕会不会死，L3 的判决量）·
`fight`（这一场从**开局**打完会怎样）· `deck`（**一次推演都不跑**，
只报内核看见了什么）。

### 它**故意**不做的三件事

| 不做 | 为什么 |
|---|---|
| 不认牌名以外的游戏文本 | 牌名 -> 内核的牌只有 `synth::card_from_name` 一处实现（和 `replay::sync` 共用），这里再写一份就是两份 |
| 不建路线 | 路线图第一刀就划在外面。`rooms` 由调用方给，**连同"它是怎么来的"回显在报告最上面** |
| 不挑候选 | "奖励屏上是哪三张"是局外信息，Python 侧读得到，内核读不到 |

### 主信号是 0 的时候**另开一栏**，不是沉默

死亡率是判决量，而**基准必死**的牌组上它恒等于 0 —— 那正是旧 advisor
「ΔHP 在死亡处被截断」那条老毛病换了个地方犯。出路不是把两个量混成一个分数
（那就是老毛病本身），是另开一栏，**只在主信号是 0 的时候印**：

* 整幕：两边都死的链上，候选**死得更靠后**（多走了几间）的对数
* 单场：两边都死的样本上，候选结束时**敌人剩的血更少**（差一口气 vs 打不动）

冒烟时它当场兑现了一次：起手牌组打第 1 幕 Boss 两臂都是 64/64 死，
而「基础打击全升」在 63/64 条配对样本上把敌人剩血压低了 **21 点** ——
主信号一个字都说不出来的地方，这一栏说得出方向。

### Python 那一侧只做翻译，但有两件**内核办不到**的事

1. **「这是哪一幕」**：内核的遭遇表按幕名索引，而 `run.act` 只有 1/2/3 ——
   **第 1 幕有两个**（Overgrowth 和 Underdocks 同序号）。唯一分得开的是
   **地图屏**报的 Boss id，而卡牌奖励屏上没有地图块。所以 `advise_core`
   看见地图就记一份到 `traces/_advise_ctx.json`，看不见就用那份缓存
   （换幕、或者**楼层倒退**——那是新开了一局——就作废）。
   **两个都没有就说认不出来**，不挑一个自洽的幕名。
2. **「还剩几间房」**：默认路线按地图上 `boss.row − 当前 row` 截尾
   （一行一间房）。**它是个 `[判断]`**，和 `bin/act_eval` 的默认路线同源，
   所以每份报告都把它和"它是怎么来的"一起印出来。

### 它**不进那九条验收**

`synth_audit` / `fight_eval` / `act_eval` 是**验收台**：测试集是实录，判据是红不红。
这个是**生产入口** —— 测试集是"现在这一局"，没有红绿，只有一份带着缺口清单的报告。
它的正确性由它调用的那三层的验收背书；它自己那一层（请求解析、候选落盘、排序）
由单测和一次离线冒烟（喂一份手搓的实况 JSON）盯着。

---

## 验收：`cargo test` + 九次对拍运行

**改完内核每次都要跑全套，不只是 `cargo test`。**

```bash
cargo test --release
cargo run --release --bin verify  -- traces/act*.json traces/synthetic_*.json
cargo run --release --bin verify  -- traces/act*.json traces/synthetic_*.json --per-turn
cargo run --release --bin verify  -- traces/act*.json traces/synthetic_*.json --predict-enemy
cargo run --release --bin solve   -- traces/act*.json
cargo run --release --bin rollout -- traces/act*.json
cargo run --release --bin rollout -- traces/act*.json --policy fast
cargo run --release --bin synth_audit -- traces/act*.json traces/synthetic_*.json
cargo run --release --bin fight_eval  -- traces/act*.json traces/synthetic_*.json
cargo run --release --bin act_eval    -- traces/act*.json
```

**第九条（`act_eval`，2026-09-12 加）判红，判据和第八条同一条：截断率必须 0**
（这里的分母是**链**不是仗）。它是 L3 阶段 3 的验收台，另外独占**回溯检验**
那一栏 —— 那一栏不判红，它报的是**偏的方向**（今天：预测每场比实测多掉 6.9 血）。
`--upgrade-check` 另跑整幕单调性自检，`--crn-off` 是它的归因臂
（把共享关掉，看死亡那一维塌回多少），两个都慢一倍、不进默认。

> **它照出过前八条全绿的错。** 2026-09-12 这台子第一次连着打几百场，
> 当场爆栈两次 —— 势不可当跟着敌人的格挡发作（`[源码]` 有 `creature == Owner`
> 而内核没有），加上触发器打出来的伤害把钩子层数重置成 0
> ⇒ 互相点火的环没有底。逐条见
> [verified-rules 的 2.11](docs/verified-rules.md#211-触发器互相点火归属和层数2026-09-12)。
> **"没有一条验收会红"和"没有 bug"是两件事** —— 这一类环要绕很多圈才看得见，
> 而前八条最长的一条推演也只有 40 个回合。

**第八条（`fight_eval`，2026-09-10 加）判红，判据只有一条：截断率必须 0。**
它是 L3 阶段 2 的验收台 —— 把每一场仗**重新打 64 次**，报死亡率 / 血量分布 /
截断率。截断的推演战斗没打完而照样带着一个血量，那是本仓库最忌讳的
"自信地算错"。**别用 `--max-turns` 去消它**，理由见「L3：单场评估」。
`--upgrade-check` 另跑一遍单调性自检（基础牌全升，配对比），慢一倍，不进默认。

**第七条（`synth_audit`，2026-09-09 加）不判红**，它报的是「构造器缺什么」——
内容覆盖问题，和 `verify` 的 MISMATCH 不是一档。但它**独占一整类检验面**：
七条既有验收全部从 `Replayer::sync` 出发，于是「开局那一刻发生什么」
（第 1 回合的 `TurnStart`、遗物挂上来的 status、敌人的开局 status）
**在它们眼里结构上不存在** —— 语料的第一帧就已经在回合 1 里了。
它第一次跑就照出六条旧错，逐条见
[verified-rules 的 2.8](docs/verified-rules.md#28-开局那一刻六条只有合成路径看得见的规则)。

动了 planner 才要跑的第八、九、十条（第八条约 2 分半，上面七条加起来只要几秒）：

```bash
cargo run --release --bin rollout    -- traces/act*.json --policy plan --plan-depth 2 --depth-sweep 1
cargo run --release --bin rollout    -- traces/act*.json --policy plan --plan-depth 2 --alt "<改之前的那几个键>"
cargo run --release --bin rollout    -- traces/act*.json --policy plan --plan-depth 2 --set  "<改之前的那几个键>"   # 只量耗时
cargo run --release --bin plan_audit -- traces/act*.json
cargo run --release --bin plan_audit -- traces/act*.json --set "<改之前的那几个键>"   # 闸门：必须复现历史读数
```

**最后那一条是闸门，别跳过。** 把被测配置扳回改之前，`plan_audit` 的五个口径
必须**逐个数复现**上一版的读数 —— 对不上说明改动漏到了它不该碰的地方，
而那种错和"planner 有问题"长得一模一样。
阶段 3 用的是 `--set "cand-score=damage,k-certain=off,tt=off"`，
阶段 4 是 `--set "window-score=leaf"`。

第九条是**同深度 A/B**：对照策略从主策略的配置出发，只改点名的键，
所以 P5 那一栏差出来的**只可能是这次改的东西**。改 planner 不跑它，
就只有"D=2 比 D=1 好多少"这一个数，而那个数对内部实现的改动近乎不动。

**第十一条不进 CI，是问「这场仗最好能打成什么样」时才跑的**（`bin/bestline`，
只对单场仗，几秒到几分钟）：

```bash
cargo run --release --bin bestline -- traces/<一场仗>.json --seeds 16 --beam 3000 --cands 40
cargo run --release --bin bestline -- traces/<一场仗>.json --seeds 64 --policies --pol-plan "k=16"
```

它**不是策略**：束搜索看得见这条随机轨迹的未来（添柴+ 生成什么它知道），
所以报的是「至少能打成这样」的**上界参照**。它独占的检验面是
**「策略离可达还差多少」** —— 前十条全是「策略自洽吗 / 比上一版好吗」，
一条都答不了这个。第 2 幕 Boss 那次就是它把「默认 D=2 在这场仗上 13/64」
和「束搜索 64/64」摆在一起，问题才从"planner 好像不太行"变成一个可修的数。

第十条是 `plan_audit`（约 10 分钟），它独占三件既有验收全都照不到的事 ——
**候选覆盖**尤其重要：不在候选集里的线，叶评估和目标函数评得再准也评不到，
所以那一栏对下游的一切改动免疫。`--inner` 再加一条"深层那一手是不是真口径下的
最优"，但很慢（第 2/3 幕语料约 25 分钟），只在动了深层选线时跑。

> **两条运行环境的坑：**
>
> * **glob 要靠 shell 展开，验证器自己不认通配符。** 上面这些是 **bash** 命令。
>   PowerShell 不给原生 exe 展开 glob，直接跑会报 `os error 123`。
>   PowerShell 里要写成 `$f = (Get-ChildItem traces\act*.json).FullName;
>   .\target\release\verify.exe $f`。
> * **别写成 `traces/*.json`。** 那个目录里除了对拍语料还放着权威数据表
>   （`cards_catalog.json` 等），验证器会在它们身上报「不支持的 trace 版本 0」
>   然后停下。真修是把数据表挪进 `data/`，见 roadmap 的「小疙瘩」。

**为什么 rollout 要跑两遍**：P2 的方向判据只在 `--policy fast` 下上膛，见下表。

### 每一项独占的检验面

**这是这套验收的全部价值** —— 不然跑七遍也只是跑七遍。每一项都能证伪一类
别人证伪不了的东西：

| 项 | 独占什么 | 换句话说 |
|---|---|---|
| `cargo test` | 对拍照不到的单点规则 | 「头槌+ 能把自己捞回来」这种 bug **四种对拍模式全看不见**，只有单测守得住 |
| `verify` 一步预测 | 每一帧的**结果** | 观测太丰富，大部分错误当帧就抓得到 |
| `verify --per-turn` | **观测里没有、又会跨动作累积**的东西 | 牌区路由的累积错误、`CardInst.bonus`（暴走那种"这张牌本场变强"）。面很窄，别高估它 |
| `verify --predict-enemy` | 敌人**下一手是什么** | 不判"这一手打多少" |
| `bin/solve` | L2 的自洽性 | 「搜索穷尽时实战线不可能比求解线高分」—— 真出现了那不是"人打得比机器好"，是求解器有 bug |
| `bin/rollout` | **敌人在预测路径上打多少** | **前面全部沉默。** 实测过：把 `enemy_turn` 的攻击改成 −3，四种 trace 对拍模式一声不吭，只有这条红 |
| `bin/bestline` | **策略离「可达」还差多少** | 前九条量的都是「自洽 / 比上一版好」。**一个稳定地输掉某场仗的策略，在它们眼里可以全绿** —— 第 2 幕 Boss 就是这样：默认 D=2 在那场仗上 13/64，而束搜索 64/64 |
| `bin/fight_eval` | **那个局面打得完吗** | 前面每一条都在问"这一步/这一帧对不对"。它问的是**整场的结局分布** —— 而且是在**没有观测**的那条路上（构造器搭的局面）。它独占的硬判据是**截断率**：`bin/rollout` 的 P4 只覆盖语料里的回合起点，这里覆盖的是"凭牌组搭出来的开局" |
| `bin/act_eval` | **一整幕串起来** | 前面每一条最长只看一场仗（40 个回合封顶）。它连着打几百场，于是**只有绕很多圈才看得见的错**在这里才现形（触发器互相点火那两条 2026-09-12 就是它照出来的，前八条全绿）。它还独占**回溯检验**：实测战损落在预测分布的哪一档 —— 那是唯一一个把「我们这套策略」和**玩家**摆在一起量的数 |
| `bin/synth_audit` | **开局那一刻**：构造器凭牌组/遗物搭出来的局面对不对 | **前面全部从 `sync` 出发**，而 `sync` 每帧从观测重灌 status、第 1 回合的 `TurnStart` 只有 `begin_combat` 跑得到 ⇒ 这一整类规则从来没被检验过。它还独占三栏：遭遇表认不认得这场仗 · `asc::hp_range` 的区间对不对 · **开局第一手**（`--predict-enemy` 是拿观测到的第一个意图去*对齐*指针，开局那一手本身没验过） |

### 今天的读数（2026-09-12 L3 **阶段 4** 之后重跑，**78** 条实录）

> **九条全跑了，判红的两条（`fight_eval` / `act_eval`）都退 0。**
> 阶段 4 只往 `synth/act.rs` 加了两个**不用就不生效**的口子
> （`pin_boss` / 跨路线配对），所以默认路径该逐字复现 —— 而它确实复现了：
> `act_eval` 的 **3152/4992 = 63%**、截断 0、漏掉 2.5 场、回溯检验
> 42/70 与 **+6.9 血**，逐个数和阶段 3 那轮相同。`cargo test` 从 396 涨到
> **399**（+3：钉 Boss 不许扰动序列 · 钉错幕要拒绝 · 换路线走另一个入口）。
>
> **一处和上一版记的数对不上，不是这次改的**：`verify --predict-enemy` 的
> 允许集合平均今天是 **1.13 手/次、9 例落在集合外**，而下面那块（2026-09-10 抄的）
> 写着 1.11 / 8 例。这一轮改动一行都没碰 `replay` / `step`
> （`git diff --stat` 只有 `synth/act.rs`、`bin/act_eval.rs` 和 `lib.rs` 的测试），
> 所以它是 **09-12 上午那两条 L1 修复**（势不可当归属 · 触发器伤害的层数）
> 带来的 —— 那两条会改触发链，而允许集合读 `enemy_hist`。
> 上一版"前八条一个数都没动"那句话在这一栏上没核到。
> **83%（340/407）和「0 只对不齐/未知」两个数是对得上的。**

```
act_eval        78 副牌组 × 64 条链 = 4992 条（2.4 秒）：**截断 0/4992**（硬判据）
                走完这一幕死掉 3152/4992 = **63%**（换种子基 3190 / 3112 —— 稳）
                逐副牌组死亡率：0–25% 的 7 副 · 25–50% 的 18 · 50–75% 的 24 ·
                  75–100% 的 24 · **100% 的 5**
                覆盖率 Overgrowth 15/22 · Underdocks 11/20 · Hive 17/20 · Glory 10/18
                  ⇒ **平均每条链漏掉 2.5 场**（抽到了但内核开不出来）
                  —— 那几场没挨打，**63% 这个数因此是乐观的**
                回溯检验：71 条打完了的实录（7 条不算：录到一半 / 末帧没有 player 块）
                  包得住 p10–p90 **42/70** · 整个 [min,max] 62/70
                  包不住的方向：**实测更省血 25** / 更费血 3 / 预测全灭而玩家活下来 1
                  **每场偏 +6.9 血**（预测均值 15.0 − 实测 8.1）
                整幕单调性自检（`--upgrade-check`）：救回 529 / 反而死 66 = **8.0:1**
                  `--crn-off` 同一批：1138 / 712 = **1.6:1** ⇒ 差出来的全是**方差缩减**
```

### 上一版的读数（2026-09-10 L3 阶段 2 之后，**78** 条实录）

**别抄，跑一遍。** 这个仓库有过抄早了一版数字的先例，而且不止一次。

> **这一轮的分母变了两次**：语料 70 -> 78（新一局第 1 幕 8 场），而且那 8 场里的
> **9 只敌人全部进表了**，于是好几栏的分子分母**一起涨** —— 和上一版逐个数比之前
> 先看分母。

```
cargo test               lib 套件 **387** 通过（+7：单场评估的七条守卫 —— 拒绝作答 · CRN ·
                确定性 · 截断不进分布 · 配对前提 · 单调性 · 偏差从牌组算出来）
语料             **78** 条实录 + 3 条合成（+2 条被动，不进默认 glob）

一步预测        **1 帧**不一致 —— 只剩那条**修不好的老账**：
                `act1_f8_elite_terror_eel` 帧0，trace 录在附魔补丁之前
                （带灵巧的耸肩无视，游戏 10 内核 8）。**只能重新打一场录**
                这一轮从 8 帧降到 1 帧，修掉的 7 帧全在新语料里，各归一条真规则：
                · 9 只新敌人进表（内容缺失，不是规则错）
                · **呼唤**（灵魂异鱼塞的状态牌）走 `Op::LoseHp` 不是 `TakeDamage`
                  —— [源码] 那一句带 `Unblockable`，身上 8 点格挡照样掉 6 血
                · **彼岸咆哮自己不消耗**（`exhausts: true` -> `false`），
                  打出去进弃牌堆；从消耗堆自动打出**只有一次**，不是永动机
整回合预测      2 帧不一致 —— **和上一轮逐字相同**（同一件事：带灵巧的耸肩无视）
回合开始手牌张数  软差异 **实录语料 0 处**（合成语料 1 处：`synthetic_smoke` 那副 4 张的
                牌堆，抽牌堆见底，不是规则差）
敌人 AI         **340/407 = 83%** 完全一致，另 44 只对上类型，**0 只对不齐/未知**
                （上一轮 308/372 + **12 只未知** —— 那 12 只就是这一轮进表的 9 只新敌人）
                允许集合平均 1.11 手/次 · 8 例落在允许集合外
L2 验收         **294** 个回合：一致 68 · 并列 27 · 换序 0 · 另选 199 · 跳过 83
                **0 例实战线赢过穷尽搜索**；17 个回合没搜完（预算 200000）
rollout solver  P1 **196/200** · P4 **0/20672** · 硬判据全过
rollout fast    P1 **196/200** · P4 **0/20672** · 硬判据全过
                （上一轮 177/181 —— 分子分母一起涨，涨的是新语料那 8 场：
                 敌人进表之前它们整场被「认不出敌人」跳过）
synth_audit     **81** 条语料（78 实录 + 3 合成）：逐字段一致 **79** · **有差 0** · 跳过 2
                （六批之前是 61 / 18 / 2）
                17 条语料带着**改牌组的遗物**（风箱/骨茶/碎石者），**没喂给构造器** ——
                第 0 帧的牌组已经被它们改过，照喂进去构造器会再升一次（见 `DECK_REWRITERS`）
                构造器自己报的缺口：只剩石化蟾蜍 2 条（开局塞一瓶药水）
                遭遇识别：唯一 **67** · 构成含随机 11 · 表里没有 1（合成假人）· 敌人不认识 **0**
                血量区间：落在区间里 **118** · **区间外 0** · 没有区间 **2**
                  （只剩合成假人和实验体 —— 后者是 `data/ascension.json` 里点名的
                   「判不出种类」欠账）
                开局第一手：默认那一手就中 **110** · 在允许集合里 10 · 类型对 0 ·
                  **集合外 0** · 允许集合平均 **1.27** 手/只
                  （上一版这里抄成了 1.19 —— 同一栏另外四个数都对得上，是抄错的）
                遭遇表覆盖率：Overgrowth 15/22 · **Underdocks 11/20**（原来 4/20）·
                  Hive 17/20 · Glory 10/18
fight_eval      79 场 × 64 样本 = 5056 条推演（1.1 秒）：**截断 0/5056**（硬判据）
                拒绝作答 0 · 跳过 2（和 synth_audit 同两条）
                死亡 533/5056 = 10.5%（换种子基 538 / 524）
                逐场死亡率：0% 的 58 场 · 0–10% 的 6 · 10–50% 的 8 · 50–100% 的 5 ·
                  **100% 的 2**（`act1_f17_boss_soul_fysh` 和一条**录到一半**的
                  `act2_f33_boss_crusher`。前者拿 `bin/rollout` 从 `sync` 局面推
                  **同样 64/64 死** —— 两条路对得上 ⇒ 差的是策略不是合成）
                单调性自检（`--upgrade-check`，基础打击/防御全升，三个种子基）：
                  血量 79 场里为正 **69–73**、均值 **+2.3～+3.1** ← 方向稳
                  死亡 救回 93–99 / 反而死 36–60 ← **噪声大得多**，见「CRN」那一节

size_of::<State>()  **3488 字节**（`N_STATUS` 128 -> 144：一天加了 10 个 status，
                119 顶到 129。`state_is_a_value` 的 4 KB 上限还有余量）
bench           单线程 **3.12–3.19 M steps/s** · 32 线程 47.7 M
                （上一版是 3.31–3.44。**掉的这 4–6% 有两个来源**：`open_hand` 多了一次
                 `fire`（`Hook::HandDrawn`，每回合一次、planner 里每个机会节点一次），
                 以及 `N_STATUS` 128 -> 144 让 `State` 从 3296 涨到 3488。
                 连跑三次谱很紧（3.12/3.16/3.19），不是噪声。
                 **已知的第一步优化是「按 hook 给 `POWERS` 分表」** —— 现在是整表线性扫）
--- 下面几栏这一轮**没跑**：planner 一个键都没动。**但它们的分母已经旧了两轮**
--- （69/70 条语料，而现在是 78，而且内容表涨了一大截）。**下次动 planner 必须连着重跑**：
rollout plan    D=2：P3 182/225 · P5 配对 18496 对，中位 0 / 均值 +4.8 · P4 0/18496
plan_audit      确定性起点 53 · 窄根 53/53 · 候选集平均 42.5 条
                候选覆盖：加窗口(A2) 8/53 · 满预算 5/53 · 到边界 6/53
                到边界后悔：A0 18/53 · 单层 18/53 · A2 现行 12/53（下限 miss_c = 6/53）
                置换表 probes 144765 / hits 7377 = 5.1% · 深层单回合搜索 22% 没搜完
seed sweep      换种子不变 228/334 = 68%（2026-09-06，69 条语料）
rollout plan A/B  ① 深层目标函数 均值 0.0 · ② 确定性窗口 −0.1 · ③ 候选生成三步 **+1.1 血**
                ④ 窗口内目标函数 +0.1 · ⑤ 深层预算 ×16 **+0.1**（阶段 3.5 的上界，判死它的就是这个数）
bestline        第2幕Boss 无厌沙虫（束宽 3000 · 40 条候选 · 16 个种子）：**16/16 赢**，
                战损 最小 7 / 中位 21 / 最大 45（起点 54 血；玩家实战那把是 26）
                同一批种子上的策略：单回合 6/64 · D=2 默认 13/64 ·
                D=2 `leaf=rollout:16:24,k=16,power-reserve=4` 18/64
```

> **「verify 从 8 帧降到 1 帧」这句话要连着下一句读**：降下来的 7 帧里，
> 5 帧是**内容缺失转绿**（敌人进表），2 帧是**真规则错被钉死**（呼唤的不可格挡、
> 彼岸咆哮的消耗）。**补内容会先把旧错误照出来再转绿** —— 这一轮两件事都发生了，
> 而且是同一批语料。

> **planner 那两行 9-01 是 `P3 157/196` / `均值 +4.2` / `seed sweep 88%`。**
> 掉下来的那部分是**作弊分**，不是回归 —— 机会节点原来在 25% 的回合边界上
> 直接把真实的下一手当成唯一的孩子。见上面「机会节点分三条路」。

> **`rollout fast` 那条红已经归因完了（2026-09-03）**：瀑布巨兽在弱策略下的
> 长尾，不是挂死，**和地道虫无关**。全部证据（含 8 个种子 × 5 档上限的表）在
> verification-log。**还没定的是要不要抬 `PRODUCTION_MAX_TURNS`** ——
> 在定下来之前 CI 把它挂在「已知红 + 守不许变多」那一档，基线 1。
> 语料涨到 64 条之后这一次采样上它是 0，**基线不跟着往下调** ——
> 长尾的 0 不是"修好了"，下一次换种子它还会回来。

### 读这些数的五条

* **「结构上对」和「买到血」是两件事，别用前者去解释后者 —— 但这条不是死路，
  它是一张诊断单。** `deep_score` 那次把深层选线的错误关掉了 **75%**
  （4795 -> 1205），端到端 P5 同深度 A/B **均值 0.0**；确定性窗口那次自己那一栏
  22/45 -> 21/45、中位 915 -> 700，端到端 **−0.1**（325/349，噪声内且方向略负）。
  两次的诊断是同一句：到边界口径的最优根线有 **18–21/45 根本不在候选集里**，
  把一个被剪短了的候选集估得更准、搜得更深，都兑现不出来。
  **2026-09-05 阶段 3 照这张诊断单去修候选集，端到端当场从平局变成 +1.1 血**
  （3221 好 / 813 坏，≈4:1）—— 前两次不是白做的，它们是这一次的前提
  （筛子换成的那把尺子正是阶段 1 对齐好的那把）。
  **两个数都要记，只记好看的那个就是在骗下一个人**；而连着记三次，
  就能看出瓶颈在哪一层。
* **读数 3 判得了"搜索够不够好"，判不了"目标函数对不对"。**
  它量的是「A2（浅、按 `Plan::budget`）离 C（深、满预算）差多少」，
  **前提是两边同一个目标函数**。阶段 4 换的正是目标函数本身，于是它当场失效：
  两把尺子只在"赢下来的终局"上差一个常数 ⇒ 阶段 4 只可能改动**涉及赢的排序**
  ⇒ **不含 win 那一列结构上就看不见它**（实测两边逐字相同：n=8 均值 3346
  中位 1200 最大 14807），而含 win 那一列裁判和被测对象**按定义**不同意。
  这类改动的判据只有整场结局：**P5（最终血量）+ P4（截断率）**。
  路线图当初给阶段 4 定的判据就是定错在这里，教训记在 verification-log。
* **`plan_audit` 读数 3 有个下限，别拿它和 0 比。** 到边界最优根线不在候选集时
  后悔必然 > 0，所以「后悔>0」最低只能降到 `miss_c`（阶段 3 之后 6/45，
  之前是 20/45）。报告里会把这个下限和"离下限还有几个起点"一起印出来。
  **那个差才是目标函数和深度还能动的部分** —— 阶段 2 结束时是 1 个，
  阶段 3 之后是 4 个。候选生成先做完，下游两阶段才有东西可买。
* **同一次改动里，「个数」和「幅度」可以往相反方向走。** 阶段 3a 把后悔>0 的
  个数从 21 降到 20，而不含 win 的中位从 700 涨到 **1200** —— 留下的那批起点
  换了一组，剩下的更难啃。**只报个数就是在挑好看的那个。**
* **「一致 46」不是独立证据。** 那一栏会涨只是因为我照着 `--live` 打 ——
  两条线当然更容易重合。**判据始终是「0 例实战线赢过穷尽搜索」那一行。**
* **「换序 0」不是求解器退步了，是语料的构成变了。** 那一栏只在我自己也选对了牌、
  只是顺序打错时才会亮；第 2/3 幕求解器几乎总能找到一堆**不同的**牌（另选 134），
  换序没有机会出现。
* **敌人 AI 那几行必须一起读。** 集合越大越容易命中，把集合开到全表也能拿 100%。
  **1.10 就是那个百分比的含金量。** 真正该看的是**落在集合外 / 未知 / 对不齐**
  这三个数 —— 其中**落在集合外今天是 6 例、对不齐是 1 只**，都不是 0，
  别把它读成满分。
  （这个百分比**口径换过两次，不能和历史值直接比**，见 verification-log。）
* **P3「策略线 ≥ 实战线」跌了不一定是回归。** 它用**单回合目标函数**给线打分，
  而能力牌按定义在那个口径下就是亏的 —— 策略开始打能力牌，这一栏必然跌。
  **判据是 P3(a)(b) 那两个 0。**

### 全绿不等于内核对了

它只说明**语料照得到的地方**没有分歧。已知还欠、而且**偏向都是乐观**的：

* 永世沙漏「剧烈增强」的力量递增（真实 3/4/5…，内核恒 +3，欠一个非观测量的计数器）。
  **2026-09-06 实测钉住了**：同一场里 Boss 力量 0 -> 3 -> **7**，第二次给的是 +4。
  从"照源码推断"升格成 [实测]，欠账本身没变
* 雾菇的复活（**欠定**：分不出是雾菇重新召唤还是它自己的 `ILLUSION_POWER`，
  按规矩留空）
* `KNOWN_UNMODELLED` 里那 10 条（见下）

**哪些东西一次都没被数据碰过，见 verification-log 的「尚未被任何数据碰过」——
那份清单比这里的绿灯重要。**

### 两条别踩的

* **录完 trace 当场跑全套，别信文档里的绿灯。** 2026-08-29 那次两帧红躺了一天多，
  就是因为录完没跑、而文档写于那之前。
* **"这一帧绿了"不等于"这条规则对了"。** 修坚定不移时青蛙骑士那一帧转绿，有一半
  是同步清零**凑巧给对了答案**。同理，**内容覆盖率 ≠ 行为覆盖率** ——
  补内容会把旧错误照出来（敌人不在表里时整帧算「内容缺失」，一补就红）。

### 被动语料不进默认 glob

`traces/passive_*.json`（玩家自己驾驶、动作是**反推**的）**证据档次比驱动语料低
一档**，`act*.json` 不匹配它。要判得显式写文件名，**读它的红先怀疑动作**。

---

## 内容清单（**别手改，跑脚本**）

```bash
"D:\game mod\sts2core\.venv\Scripts\python.exe" tools/count_content.py --md
```

**这张表是 `tools/count_content.py --md` 的输出，整块替换掉，不要手改一格。**
2026-09-05 建这个脚本时它当场抓到两处：上一版手写的 `CARDS` 是 114（实际 **115**）、
合成语料写 2 条（实际 **3** 条）—— 而上一版就是「2026-09-03 从源码重数」的，
**两天就漂了**。这类计数手改必错，已经错过至少四次。

| 表 | 内核 | 权威表 |
|---|---|---|
| `CARDS` | **117** 项 | 128 —— 覆盖 **112/128**，缺 16 张 |
| `ENEMIES` | **82** 项 | wiki 115 只 |
| `POWERS` | **84** 条触发规则 | — |
| `RELICS` | **104** 件（`modelled` 87 真 / 17 假）| 97 件 |
| `POTIONS` | **23** 个槽 | 已发现 40 瓶 |
| `ENCHANTS` | **22** 种（`modelled` 5 真 / 17 假）| [源码] 22 种 |
| `KNOWN_UNMODELLED` | **8** 条豁免 | — |
| `SYNTH_ONLY_GAPS` | **4** 件遗物只在合成路径上欠 | 见 `synth::Gap` |
| 语料 | **78** 条实录 + 3 条合成 + 2 条被动 | 实录**不可再生** |

读这张表的三条：

* **`ENEMIES` 含 3 只合成假人 + 1 个 `<未知敌人>`**；`CARDS` 含 1 个 `<未知牌>` 占位。
* **`RELICS` 的「进表」不等于「建模」** —— `modelled` 那一列才是。分类（哪件卡在
  哪个钩子上）见 [`docs/relic-hook-taxonomy.md`](docs/relic-hook-taxonomy.md)。
* **`RELICS` 那一行现在是 98 对 97，分子大于分母 —— 这本身就是快照过期的证据**：
  内核有佩尔之血和尖叫酒壶，09-01 那份快照两件都没有（都是之后捡到的），
  反过来快照有硫磺而内核没有。**分子分母数的不是同一批东西**，
  只看数会读成"建完了还超额"。**权威表该重导**（`tools/dump_relics.py`）。
  （2026-09-06 之前这里是 97 对 97 —— 那个相等本来就是巧合，两边各差一件。）
* **`ENCHANTS` 同理**：22 种是 [源码] 里的全部，`modelled` 那一列才是建了几种。
  没建全的每一条都在 `note` 里点名卡在哪，而且**照样会被点名报出来**
  （`Report::unknown_enchantments`），不会静默算错。
* **`KNOWN_UNMODELLED` 是欠账清单不是白名单**：语料天天碰到、内核天天算错，
  而对拍不报红。每条各自写着卡在哪。
* **`SYNTH_ONLY_GAPS` 和 `RELICS` 的 `modelled` 问的不是同一件事。**
  那一列问「**对拍**路径上够不够」（那条路上手牌/血量/药水槽全是观测量，
  `sync` 每帧照抄），这一张问「**合成**路径上够不够」（没有观测可抄）。
  **同一件遗物可以对拍够了而合成不够** —— 风箱、骨茶、宝石面具、小血瓶、
  石化蟾蜍五件就是这样。两条路两个覆盖率，别只报一个。

脚本的口径（和两个踩过的坑）写在 `tools/count_content.py` 的文件头。

**权威表要定期重导**（`tools/dump_catalog.py` / `dump_relics.py` / `dump_potions.py`）。
它不是常量，是「这个存档发现了多少」的快照 —— 对着旧快照会**高估**自己的覆盖率
（2026-08-30 重导时牌表一次涨了 22 张）。分子（建了几件）和分母（发现了几件）
是两件事，**只有前者是工作量**。

### 缺的 16 张牌，各自卡在一个还没有的机制上

乱战 / 勒紧 / 发现 / 延伸 / 心灵腐化 / 心灵震慑 / 放松 / 无休手斧 /
未掘宝石 / 深谋远虑 / 神气制胜 / 秘密技法 / 秘密武器 / 许愿 / 金斧 / 闪亮登场

（**彼岸咆哮 2026-09-06 建掉了** —— 它欠的"回合末从消耗堆里自己打出来"
现在是 `EXHAUST_END_AUTOPLAY` 一行表。它是这一批里第一个被建掉的，
起因是一条**假装成规则错误的内容缺口**：原始力量+ 该把它变成巨石+，
而 `<未知牌>` 的 `kind` 不是攻击。）

典型的几个：**发现/秘密技法/秘密武器**欠「从生成的候选里挑一张」那种 `Pending`
（和无色药水同一个）· **放松/延伸**欠「下个回合才发作」的一次性 `TurnStart` status ·
**心灵腐化**欠抽牌数修饰器（清晰药水同一个洞）· **心灵震慑**欠「伤害 = 抽牌堆张数」
这个 `Op` **且**欠固有（Innate）· **未掘宝石**欠重放（Replay）关键字。

**不是"忘了填"。** 完整的一张表在
[verification-log](docs/verification-log.md#补内容盾墙9-张牌5-件遗物2026-08-30)。

### 没建的那 20 件遗物（现数走 `count_content.py`，别手写）

2 件卡在「施加 status 时插一手」的钩子（损毁头盔/不安油灯）· 3 件卡在 L1 没有的
局外信息（房间类型）· 其余各欠各的，`RelicDef::note` 里逐件写着。

**权威表里还有一件内核没有的：硫磺**（`BRIMSTONE`，回合开始给自己 +2 力量、
给全体敌人 +1）。它是 `RELICS` 那一行「97 对 97」看着满、其实两边各差一件的另一半。

**曾经写着"5 件卡在附魔系统"那一格 2026-09-06 拆开了**：附魔系统建起来之后，
扭曲锤子和王室印章**本来就不欠战斗层什么** —— 它们的效果全在局外（拾起时给牌
附魔），战斗层由 `CardInst` 上的附魔承载，而附魔是观测量。剩下三件各欠各的：
木札欠它给的那种附魔（娴熟）· 神秘打火机欠自己那个 `ModifyXxx` 钩子 ·
幽灵种子欠的根本不是附魔而是**虚无关键字**。
**"卡在同一个洞"是个偷懒的归类**，拆开才看得出三件事。

### `ModifyXxx` 那一族：**不需要新表，需要遗物把状态交给实体**

「改一个正在算的数值」的遗物（打击木偶/钢笔尖/纸蛙/化学物X…）曾经被记成
"现有三类表里没有位置，硬加就得往 `damage.rs` 塞 `if 有没有某遗物`"。
**2026-09-06 钢笔尖建掉了，出路是不往那边走**：

> 遗物把自己的状态交给**玩家实体**（`RelicDef::private_status` + `counter_to`
> 给的 status），`damage.rs` 照旧**只认 status** —— 和它已有的十个乘区一模一样，
> 一个 `if 遗物` 都没有。

钢笔尖用了三个：`PenNib`（在场）· `PenNibCount`（跨战斗计数，走 `counter_to`
从观测的遗物面板灌）· `PenNibArmed`（这一次出牌翻倍，进 `PIPELINE_STATUSES`）。
计数在 `step::resolve_played_card` **结算之前**（[源码] `BeforeCardPlayed`），
乘区在 `damage::apply_modifiers`。同族剩下的 7 件照这个套路都建得进去。

### `KNOWN_UNMODELLED`：欠账清单，8 条

它是哑弹测试的**豁免清单**，每加一条就是承认一处内核看不见的机制。
**这张表是欠账清单不是白名单** —— 加之前先问一句：是真的不建，还是只是还没建？
（荆棘在这张表里躺过：挂着、观测对得上、没有任何规则读它，内核因此少算了每一次
反弹伤害，而**没有任何测试会发现**。2026-08-29 建掉了。）

真正欠着的四条，各自点了名卡在哪。**两天之内建掉了两条**：
沙坑（2026-09-05，它欠的钩子 `EnemyTurnStart` 其实 08-30 就为敌人覆甲加过了，
豁免在过期注释后面多躺了六天）· 幻象（2026-09-06，见下）：

| status | 欠什么 | 方向 |
|---|---|---|
| `Hatch` 结实的卵 | `EOp::TransformSelf`（掉到 0 层原地变幼虫） | — |
| `PersonalHive` 私有蜂巢 | 「把 powered 传进 `EnemyDamaged`」+「随机位置塞 N 张」 | — |
| `Reattach` 重接（千足虫） | 「尸体留在场上 + 死后仍走出招表」 | **乐观**：内核低估这一场 |
| `PainfulStabs` 剧痛刺击 | 「把"这一下打穿了多少"传进钩子」（现有 `Attacked` 只说挨了一下） | **乐观**：内核少给我塞伤口 |

另外四条（`Minion` / `Illusion` / `EscapeArtist` / `Swipe`）**性质不一样，别一起读**：

* `Illusion` **2026-09-06 建掉了**。它在这张表里挂着"欠定"（分不出是复活还是
  雾菇重新召唤）——**而那个欠定是可以查清的，只是一直没去查**：
  反编译里 `IllusionPower.AfterDeath` 写得清清楚楚，而同一份实录
  （`act1_f15_ninth`）里雾菇那两个回合的意图**根本不是 Summon**。
  **源码 + 已有语料就能结掉的"欠定"，不该躺在欠账表里** —— 这条教训比规则本身值钱
* `Minion` 只是个标签，`step.rs` 真的在读它（爪牙随主死 + 选目标）
* `EscapeArtist` **已经有规则了**（`EnemyTurnEnd` 的倒计时，见钩子表）——
  这条豁免是建之前的残留，**清理它是安全的**。真正没建的是「逃跑之后离场」那一半
* `Swipe` 没有任何说明，**先查清楚再动**

> **这份欠账在 2026-08-29 之前不可枚举** —— 只有读遍 `state.rs` 才知道欠了什么。
> **挑下一个要建的从这张表里挑**，但先按上面那条分清哪几条是真欠账。

---

## 实战前必须知道的三个洞

1. **`sync` 不还原 `Pending`。** 观测里有 `pending` 字段，但同步只把它当布尔量读。
   游戏开着选牌界面时内核会以为可以随便出牌，给出一条游戏根本不接受的线。
   `solve --live` 现在**遇到 `pending` 直接拒绝作答** —— 这是止血不是修好。
   真修要把选牌界面的候选和剩余张数一起同步进来。
2. **抽牌只有当前这一堆是确定的。** `draw_pile_order` 补丁之后 `sync` 整堆照抄、
   一个随机数都不掷、`n_draw_known` 拉满。**仍然猜的那一半**：抽牌堆抽空之后那次
   洗牌的结果不可知（游戏的 `Rng.Shuffle` 状态没暴露）。老 trace 没这个字段，
   照旧走"内容当多重集、顺序自己洗一个样本"。`Line::drew` 把这件事标出来。
   抽牌还会**让换序合并失效**（局面不再逐字节相同），带抽牌的手牌搜索规模是
   阶乘级的 —— 有测试钉着。
3. **预算用光就退化成启发式排名**，`Solved::complete` 报出来。**这真的会发生**：
   今天 220 个回合里 8 个没搜完。手牌一大，**"穷尽搜索"这个前提就开始失效**，
   连带那条"实战线赢不过求解线"的硬自检在那几行上也只是启发式比较。
   **读 `--live` 时看 `complete` 那一列。**

`--live` 会把手里它不认识的牌、身上它看不见的遗物（连同卡在哪）都点名列出来。
**那几行不是装饰。**

> **一个刚补掉的洞值得单独记：内核每回合少发一张牌。** 佩尔之血
> （`ModifyHandDraw => count + 1`）不在内容表里，而摆动球的相位又早了一个回合
> —— 两个错互相抵消成「±1 的噪声」，全语料 64 处软差异被当噪声读了很久。
> 三条规则一起建全之后那 64 处**归零**。教训写在
> [verified-rules 的 2.6](docs/verified-rules.md)：**系统性错误可以长得像噪声。**
>
> **第四个洞 2026-09-06 补掉了一半，剩下的一半要知道。** `sync` 从战斗**中途**
> 接进来时（`round > 1`，而实战驱动**永远**是中途调用）原来**整块跳过遗物**，
> 于是第 2 回合起内核一件遗物私有量都看不见 —— 燃烧之血、锚、招架盾、钢笔尖
> 全没有。现在只跳过「一场只用得掉一次」的那些
> （`content::spent_once_per_combat`，判据是它自己的规则里有没有 `ClearSelf`）。
> **剩下的那一半是有意的**：臂甲的翻倍和百年积木的抽牌在中途接入时仍然
> 当成已经用掉 —— 前面发生过什么不可知，宁可低估自己。

---

## 加规则之前：先去看玩家给的判定

卡面文本对触发类交互是**欠定的**。遇到「每当…时」/多目标/牌堆顺序这类判定，
**先问用户，不要挑一组自洽的解写进去** —— 他一次纠正过我三个凭卡面推的错结论。

已有的判定表（绯红披风+撕裂、闪电霹雳+凶恶、恶魔之焰+黑暗之拥、拳斗、全身撞击、
熔融之拳、万向斩、火焰屏障、灼伤走不走格挡、完美打击数哪些牌……）在
[verified-rules 的第 2 节](docs/verified-rules.md#2-玩家判定卡面读不出来的那些)，
**大部分有测试钉着 —— 万向斩 / 完美打击 / 熔融之拳这三条没有**，见那一节的 2.1。

---

## 文件

### `src/`

| 文件 | 内容 |
|---|---|
| `state.rs` | POD 状态、状态位 `St`、牌区、RNG、抽牌/洗牌、抽牌堆的**已知前缀** `n_draw_known`（+ 收口点 `pop_draw_top`）、卡实例 `CardInst`（8 字节：id/flags/bonus/cost_delta/**ench**/**ench_amt**）。`MAX_ENEMIES 5` / `N_STATUS 128` / `MAX_POTIONS 10` |
| `damage.rs` | **唯一**伤害管线 + 格挡 / 人工制品 |
| `asc.rs` | **生成产物，别手改**（`tools/dump_ascension.py`）。进阶数值两档：`ASC_HP`（血量区间）+ `ASC_OPS`（招式数值）+ **唯一的收口点 `adjust`**。见「进阶」那一节 |
| `ops.rs` | `Op` / `EOp` / `CardDef` / `EnemyDef` 定义 + `POTIONS`（表头列着故意不建的那些各自欠什么机制）|
| `content.rs` | `CARDS` / `ENEMIES` / `POWERS` / `RELICS` / **`ENCHANTS`**（附魔，含 `enchant_by_id` 和三个取值函数）/ `GEN_POOL` 生成池 / `TURN_SCOPED` / `RULE_MODIFIERS` / `HAND_END` / **`EXHAUST_END_AUTOPLAY`** / `KNOWN_UNMODELLED` / `X_COST_CARDS` / `UNPLAYABLE_CARDS`（判据是 [源码] `CardKeyword.Unplayable`，不是"有没有 ops"）|
| `step.rs` | `step` / `legal_actions` / `begin_combat` / `end_turn_with_incoming` / `fire` / `exhaust_card` / `gain_block` |
| `json.rs` | 最小 JSON 读取器（为了保持零依赖）|
| `replay.rs` | 对拍：观测同步 + 逐字段 diff。`Replayer::sync` 也被 L2 的验收复用 |
| `solver.rs` | **L2 单回合求解器**：`Threat` / `Line` / `solve_turn` / 目标函数 / 药水定价 / 局面指纹 `key`（Zobrist 风格）|
| `plan.rs` | **跨回合 planner**（限深 expectimax）：机会节点 `chance_children`（超几何枚举 / CRN 采样）· 确定性窗口 `window_is_certain` · 候选生成（`cand_score` / `k_at` + `root_is_narrow`）· 跨候选 memo（`plan::key` + 无锁 `Tt`）· `Leaf` 枚举 · `power_reserve`。**`Plan::apply` / `Plan::describe` 也在这里** —— `--alt` / `--set` / `--plan-set` 三个验收台共用一份键解析 |
| `rollout.rs` | 跨回合推演：`Policy`（`Fast` / `Solver` / `Plan`）+ `predicted_threat` + `close_pending` + `Outcome` + **`par_map`**（并行分块，`sample_outcomes` 和 `synth::eval` 共用 —— "结果和串行逐字相同"这条性质只该有一处守着）|
| `synth.rs` | **L3 阶段 1：合成战斗构造器**。`FightSpec` / `DeckCard` / `EnemySpec` / `Gap` / `build`。血量从 `asc::hp_range` 掷（**独立随机流**，不碰 `State::rng`）。`card_from_name` 是「游戏里的牌 -> 内核的牌」那一步，和 `replay::sync` 的 `push` 必须给出同一个 `CardInst` |
| `synth/eval.rs` | **L3 阶段 2：单场评估**。`EvalCfg` / `FightEval` / `Dist` / `Caveat` / `Refusal` / `evaluate` / `paired_delta`。**每个样本各 `build` 一次**（敌人血量是这场仗真实存在的一条方差）· 截断的样本不进任何分布 · 配对里死亡和血量**拆开**。见「L3：单场评估」 |
| `synth/from_obs.rs` | 观测第 0 帧 -> `FightSpec`：`extract` / `build_start` / `corrected_hp` / `DECK_REWRITERS`。**`synth_audit` 和 `fight_eval` 共用这一份** |
| `synth/act.rs` | **L3 阶段 3：整幕链式评估**。`Room` / `ActPlan` / `ActCfg` / `ActEval` / `evaluate_act` / `paired_act_delta`（+ 换路线那一对走 `paired_act_delta_across_routes`）/ `draw_sequence` / `rest_heal`。`ActPlan::pin_boss` 钉住局外已知的那只 Boss（钉错幕整个拒绝作答）。**路线由调用方给**（这一层不建路线）· 抽序列照 [源码] `GenerateRooms` 的分布 · **开不出来的那一场跳过并计数** · `crn_salt` 是把共享关掉的归因旋钮。见「L3：整幕链式评估」 |
| `synth/encounters.rs` | 遭遇表加载器：`data/encounters.json` + `data/enemy_ids.json` -> `Table`。`resolve`（遭遇 key -> 敌人，构成含随机的**整条拒绝**）· `identify`（场上这批敌人是哪一场）· `coverage`（这一幕开得出几场）|

### `src/bin/`

| | 干什么 |
|---|---|
| `verify.rs` | 对拍验证器（三种模式）|
| `solve.rs` | L2 验收（求解线 vs 实战线）+ `--live` 实战建议 + `upgrade_threat_live` |
| `rollout.rs` | 跨回合验收 P1–P5。`--policy` 选被审的策略，**要跑两遍**。`--alt "k=v,…"` 是**同深度 A/B** 通道（对照策略从主策略配置出发，只改点名的键）—— 没有它，P5 只答得了"D=2 比 D=1 好多少"。`--set "k=v,…"` 改**主策略**（键相同），单独跑一遍量耗时用它 —— 一个 `--alt` 进程里两条 arm 的耗时是混在一起的 |
| `plan_audit.rs` | **planner 的审计台**：候选覆盖 / 深层内层最优性 / 到边界后悔。三条都在**确定性起点**上量，裁判是"真敌人回合 + `score::leaf`"而不是 `threat.end_turn`。五个口径逐条评同一批线，其中 **A2 = planner 今天真的在做的事**（含确定性窗口），A→A2 就是阶段 2 那一栏。它还会**真跑一遍 `plan::plan_report`**，报候选集条数（和审计台自己搭的那份对不上就是台子错了）和置换表命中率。`--set "k=v,…"` 换被测配置（键同 `--alt`），`--inner` 那一条很慢 |
| `bestline.rs` | **整场战斗的上界**：跨回合束搜索。`--policies` 在同一批种子上并排跑单回合策略和 planner（**配对比较**），`--sweep` 扫 `Weights::clock` 的刻度，`--explain N` 把一整场逐回合印出来。**它不是策略** —— 它看得见这条随机轨迹的未来，报的是"至少能打成这样" |
| `calib.rs` | 标定样本导出器：每个回合起点导「叶局面 → 真实结局」 |
| `synth_audit.rs` | **合成战斗构造器的验收台**：拿实录的**第 0 帧**当测试集，构造器搭同一场仗，和 `Replayer::sync` 逐字段比。四栏读数各占一类失败（逐字段差 / 遭遇识别 / 血量区间 / **开局第一手**）。**不判红** —— 它报的是内容覆盖，不是正确性。三样结构上不可比的（手牌身份 / `n_draw_known` / `enemy_def`）写在模块头 |
| `fight_eval.rs` | **L3 阶段 2 的验收台**：拿实录第 0 帧当测试集，构造器搭同一场仗，**打完 64 次**报分布。**唯一的硬判据是截断率 0**（判红）。`--upgrade-check` 是单调性自检（基础牌全升，配对比）。它**故意不做回溯检验** —— 那是阶段 3 的判据，而 trace 末帧不保证是战斗结束、实测那场又是玩家打的，两个差混在一个数里什么都判不了 |
| `act_eval.rs` | **L3 阶段 3 的验收台**，两栏：**整幕链**（拿实录的真牌组走完那一幕 64 次，硬判据仍是截断率 0）和**回溯检验**（真牌组 + 真遭遇 + 真战损，看预测的战损分布包不包得住实测值，另报分位数排名直方图）。后一栏**不判红** —— 它量的是「玩家 + 我们这套策略」两个差的合量，判得了**偏的方向**，判不了"内核对不对"。`--rooms` 换路线（默认那条是 `[判断]`，印在报告最上面）· `--upgrade-check` 整幕单调性 · `--crn-off` 归因臂 |
| `advise.rs` | **L3 阶段 4：构筑顾问的生产入口**（**不是验收台，不进那九条**）。一份 JSON 进、一份人读的报告出；三个问法 `act` / `fight` / `deck` 共用一份请求，四个决策都翻译成「候选」。**主信号是 0 时另开一栏**（两边都死的链谁走得更深 / 两边都死的仗谁把敌人打得更残）。见「L3：MCP 接线」 |
| `bench.rs` | 吞吐基准（无外部依赖）。**这台机器读数波动很大，别拿它当基线** |

### `tools/`（全部只用标准库，不依赖 MCP）

| | 干什么 |
|---|---|
| `solve_now.py` | **实战入口**：读局面 → `record_trace.normalize()` → 单帧 `traces/_live.json` → `solve --live --plan`。**固定命令，无参数** |
| `advise_core.py` | **L3 的实战入口**：读实况 → 拼请求 → `advise.exe`。它独占两件内核办不到的事：**认这是哪一幕**（第 1 幕两个同序号，只有地图屏的 Boss id 分得开，缓存在 `traces/_advise_ctx.json`）和**数还剩几间房**。`advisor/server.py` 的八个 MCP 工具全调它 |
| `fixtures/` | 两份**手搓的实况 JSON**（不是实录）：`advise_core.py --selftest` 的测试集。一份带地图块（认幕 + 钉 Boss 走它），一份故意没有（验"认不出是哪一幕就拒绝作答"）|
| `record_trace.py` | **驱动式**录制器（它执行动作，所以动作是已知的）。`--plan` 见下 |
| `watch_trace.py` | **被动**录制器（玩家自己点，我在旁边看）。配置走 `traces/_watch.txt` |
| `reinfer_trace.py` | 反推规则改进后，拿存下来的观测把 `inferred` 的动作**重算一遍**，不用重打一场。真实动作一个都不碰 |
| `backfill_draw.py` | 从保留的原始帧把抽牌堆**内容**回填进老 trace。只增 `draw` 一个键，按指纹配对 |
| `plan_seed_sweep.py` | 量 D=2 建议的**可重复性**：同一个局面只换 `Plan::seed`，看它换不换线 |
| `calib_report.py` / `calib_power.py` | 读 `bin/calib` 的 CSV 做标定 |
| `dump_catalog.py` / `dump_relics.py` / `dump_potions.py` / `dump_bestiary.py` | 导权威表。**药水那个和另外三个不同构**：wiki 端点给不了药水，文本得从 `player.potions[]` / `rewards.items[]` 捡，只增不减 |
| `count_content.py` | **内容清单唯一的计数口径**。`--md` 直接吐上面那张表的 markdown，整块替换。**任何文档里的计数都从这里来，不要手写** |
| `enemy_report.py` | 从 trace 聚合敌人观测 → `traces/enemies_observed.json` |
| `relic_hooks.py` | 从反编译源码读每件遗物的**钩子面**（行为，不是静态字段）|
| `dump_ascension.py` | 从反编译源码导**进阶数值**（A8 敌人耐久 / A9 敌人输出）到 `data/ascension.json` + 生成 `src/asc.rs`。**按 (种类, 低进阶值) 认内核的 op**，认不准就整条跳过并报出来 |
| `dump_encounters.py` | **L3 的数据地基**：从反编译源码导「幕 → 遭遇 → 怪物」到 `data/encounters.json`，从实录导英文类名 ↔ 内核敌人的连接键到 `data/enemy_ids.json`，并印**覆盖率**（这一幕内核今天开得出几场仗）。`--md` 吐覆盖率表。**解析器敢于放弃** —— 构成含随机的遭遇标 `exact: false`，由 `data/encounters_overrides.json` 手填 |

### `advisor/`：唯一依赖 `mcp` 包的地方

| | 干什么 |
|---|---|
| `server.py` | `sts2-advisor` MCP server。八个工具名映射到 `tools/advise_core.py` -> `bin/advise` 的三个问法，**一条游戏规则都没有**。2026-09-12 从 `sts2sim/` 搬进来 —— 它是 L3 接口的消费者，接口一变它就得跟着变，放在另一个目录时没有任何东西把这两件事绑在一起 |
| `../pyproject.toml` | 只为 `uv run` 存在（`mcp` 这一个依赖）。`packages = ["advisor", "tools"]` 让 `from tools import advise_core` 成立 —— **普通 import，不是 `sys.path` 注入** |
| `../uv.lock` · `../.venv/` | 这个仓库自己的 Python 环境。**`.venv` 是所有 Python 工具的解释器**（PATH 上那个 `python` 是 Store 假壳）|

> **依赖的分界线**：`mcp` 只出现在本目录。**`tools/` 那十几个脚本一律只用标准库**
> —— 那条规矩保证录制器和实战入口不依赖 MCP，游戏跑着、MCP server 也跑着的时候
> 它们能独立工作，不跟它抢连接。

### `data/`：不是 trace 的那些表

| 文件 | 是什么 | 谁生成 |
|---|---|---|
| `encounters.json` | [源码] 幕 → 遭遇 → 怪物 + 房间构成 + 怪物血量区间 | `dump_encounters.py` |
| `enemy_ids.json` | [实录] 英文 `MonsterModel` 类名 ↔ 内核敌人 | 同上 |
| `encounters_overrides.json` | **手写**：解析器放弃掉的那几场，每条写着为什么确定 | 人 |
| `ascension.json` | [源码] 进阶数值全表，含 `unmatched` 那份欠账 | `dump_ascension.py` |
| `ascension_overrides.json` | **手写**：生成器认不出的那些填这里（今天是空的） | 人 |
| `enemies_observed_legacy.json` | **[实测] 敌人最大血量**，2026-08-14 那一局边打边记的（旧 advisor 的 `bestiary.json`，2026-09-12 随它退役搬进来）。**35 只里 22 只对拍语料里没有** —— 语料 08-16 才开始，那一局打到第 3 幕却一帧没录。用处：对 `asc::hp_range` 那张 [源码] 档的表、补敌人内容时有个实测起点。四条注意事项（进阶不明 / 幕和种类是旧口径 / 名字带局内后缀 / 伤害是意图标签）写在文件头 | 人（迁移）|

**生成的那三份连同 `src/asc.rs` 一起跟踪，尽管可重跑** ——
它们的输入是 `decompiled/`，而那个目录是 `.gitignore` 掉的：
不跟踪产物的话，clone 下来的仓库重跑不出这几份表，而 `src/asc.rs` 还要参与编译。

**这个目录是 roadmap 那条「把权威表挪进 `data/`」的第一块** ——
既有的 `traces/*_catalog.json` 还没搬，搬它要动 `src/lib.rs` 的路径断言和四个 dump 工具。

### `traces/` 和 `decompiled/`

`act*.json` / `synthetic_*.json` 是对拍语料；`passive_*.json` 不进默认 glob；
`*_catalog.json` / `enemies_*.json` 是**权威数据表、不是 trace**（混在一个目录里，
见 roadmap 的「小疙瘩」）；`_plan.txt` / `_watch.txt` 是两个录制器的配置文件；
`_live.json` 是生成物。

`decompiled/` 是**生成物**：`sts2.dll` 反编译出的 3425 个 `.cs`，
就是全文里 `[源码]` 这一档的来源。可再生，随手可删。

---

## 录制：两个录制器，各一条固定命令

每出一张牌换一次命令字符串，就要过一次权限确认。所以两个录制器都是
**固定命令 + 固定路径的配置文件**。

```bash
# 驱动式：我出牌，动作是已知的。动作全写进 traces/_plan.txt
& "D:\game mod\sts2core\.venv\Scripts\python.exe" "D:\game mod\sts2core\tools\record_trace.py" --plan

# 被动式：玩家出牌，动作是反推的。配置走 traces/_watch.txt
& "D:\game mod\sts2core\.venv\Scripts\python.exe" "D:\game mod\sts2core\tools\watch_trace.py"
```

计划文件每行一个步骤，语法和子命令一样（`trace <路径>` / `init` /
`play 0 --target X` / `end` / `potion 0` / `note ...` / `finish`）。跑完会**自动把
动作行全注释掉**，所以重复执行是空操作；任何一步失败都会停下并报出是第几步、
原文和原因。

被动录制器**写进 trace 的每个动作都带 `inferred: true`**，反推不出来的写
`kind="unknown"` —— **一个自信地填错的动作比一个诚实的 unknown 更难查。**
观测是不可再生的证据、动作只是推断，所以反推规则改进之后不用重打一场，
跑 `reinfer_trace.py` 就行。

两个录制器判「动作成没成」都**读 mod 自己回的 `status`**，观测差分只做兜底 ——
理由（以及那条豁免只给 `select_card`）在父目录 [`../CLAUDE.md`](../CLAUDE.md)。

---

## 内容从哪来

`get_compendium` 的 `card_library` **只有 id 和统计，没有卡面文本**，灌不了内容。
有文本的是 `GET /api/v1/wiki`，而且**同时给基础版和升级版** ——
`tools/dump_catalog.py` 走的就是这条路。它一次就纠正了 5 个照初代记忆填的 `ops_upg`
（欺凌+/烙印+/踩踏+/无情猛攻+/预备打击+）。敌人那边 `bestiary` 在 UI 里是锁的，
只能从 trace 反推，走 `tools/enemy_report.py`。

**"没实现"和"没验过"是两件事，后者的数字大得多。** 表里很多牌是 `[源码]` 灌进去的，
**第一次实战打出时对拍才会判它**。

---

## 构建与环境

```bash
cd "D:\game mod\sts2core"
cargo test --release --lib
cargo run --release --bin bench
```

工具链：rustc 1.97.1，**stable-x86_64-pc-windows-gnu**（这台机器没有 MSVC 和
Visual Studio Build Tools，GNU 工具链自带链接器，能独立工作）。
cargo 在 `C:\Users\admin\.cargo\bin`。

因为选了 GNU 工具链，Python 侧**走独立可执行文件 + JSON/stdio IPC**，而不是 PyO3
—— 反正边界调用很少（一次"评估这个决策"进去、算几十秒出来），IPC 开销可以忽略，
还能绕开 CPython 的 MSVC ABI 问题。

Python 那一侧的环境由 `uv` 按本仓库的 `pyproject.toml` 建（`.venv/`，gitignore 掉）：

```bash
uv run --directory "D:\game mod\sts2core" python -m advisor.server   # MCP 那边就是这么起的
"D:\game mod\sts2core\.venv\Scripts\python.exe" tools/advise_core.py --selftest
```

### 这个环境的 shell 坑

* 默认 shell 是 **Windows PowerShell 5.1**：**没有 `&&`**，用 `;` 或 `; if ($?) { ... }`
* **PowerShell 不给原生 exe 展开 glob** —— 见「验收」那一节的两条运行环境坑
* **不要对原生 exe 用 `2>&1`**：5.1 会把 stderr 每行包成 ErrorRecord，
  即使 cargo 返回 0 也会冒出 `NativeCommandError`。stderr 本来就会被捕获
* 路径含空格（`D:\game mod`），记得加引号
* PATH 里的 `python` 是 Microsoft Store 的假壳，**真的解释器在本仓库的 `.venv/`**
  （`D:\game mod\sts2core\.venv\Scripts\python.exe`，2026-09-12 从 `sts2sim/` 搬过来）
